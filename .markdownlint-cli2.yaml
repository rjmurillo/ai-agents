# Markdownlint Configuration
#
# This config follows Prettier compatibility guidelines to enable seamless copying
# of markdown files between projects that use Prettier for formatting.
# See: https://github.com/DavidAnson/markdownlint/blob/main/doc/Prettier.md

config:

  # ═══════════════════════════════════════════════════════════════════════════
  # PRETTIER-COMPATIBLE SETTINGS
  # These rules are disabled to avoid conflicts with Prettier formatting.
  # Reference: https://github.com/DavidAnson/markdownlint/blob/main/style/prettier.json
  # ═══════════════════════════════════════════════════════════════════════════

  # Heading style - disabled for Prettier compatibility
  MD003: false

  # Line length - disabled for Prettier compatibility
  # (Also needed for agent templates with long tool lists and descriptions)
  MD013: false

  # Ordered list prefix - disabled for Prettier compatibility
  # (Sequential numbering 1/2/3 is more readable than all-1s)
  MD029: false

  # Code fence style - disabled for Prettier compatibility
  MD048: false

  # Emphasis style - disabled for Prettier compatibility
  MD049: false

  # Strong style - disabled for Prettier compatibility
  MD050: false

  # ═══════════════════════════════════════════════════════════════════════════
  # PROJECT-SPECIFIC SETTINGS
  # These rules are project requirements that don't conflict with Prettier.
  # ═══════════════════════════════════════════════════════════════════════════

  # Table column style - disabled, tables are readable as-is
  MD060: false

  # No-space-atx heading - disabled. Issue #4118: --fix rewrites a line-start
  # issue reference (#NNNN) into an H1 heading (# NNNN). This repo uses #NNNN
  # as prose shorthand for GitHub issues; that pattern cannot be fixed
  # automatically without corrupting valid text.
  MD018: false

  # Fenced code language - REQUIRED for syntax highlighting
  # Note: 3 known false positives in nested template files (retrospective.md, roadmap.md)
  # where nested code blocks cause parser confusion
  MD040: true

  # Inline HTML - allow specific elements, flag unescaped generic types
  MD033:
    allowed_elements:
      - br
      - code
      - kbd
      - sup
      - sub
      - details
      - summary
      - code
      - strong
      - code
      - example

  # Duplicate headings - scope the check to siblings under one parent.
  # docs/installation.md intentionally repeats the same platform sub-heading
  # ("Claude Code", "GitHub Copilot CLI") under several distinct parent
  # sections (Installation Methods / Uninstallation / Troubleshooting). That
  # is readable and correct; only true sibling duplicates under one parent are
  # a problem. siblings_only restricts MD024 to siblings. Verified behavior
  # (markdownlint v0.40.0): with siblings_only true, two "### Foo" under
  # different "##" parents pass; with the default false the second is flagged.
  # Refs Issue #1837.
  MD024:
    siblings_only: true

  # Multiple H1 - allow in agent templates (frontmatter can create implicit H1)
  MD025:
    front_matter_title: ""

  # Code block style - consistent fenced style (not a Prettier conflict)
  MD046:
    style: "fenced"

  # List marker style - consistent dashes (not a Prettier conflict)
  MD004:
    style: "dash"

  # First line heading - required by default
  MD041: true

# NOTE: top-level `globs:` removed deliberately. markdownlint-cli2 ADDS
# config globs to any files passed on the command line, so a hook that
# lints one touched file (`markdownlint-cli2 --fix path/to/file.md`)
# would also walk every `**/*.md` in the repo and take minutes per
# invocation. Callers that want to lint the whole repo MUST pass
# `**/*.md` (or a narrower glob) on the command line; see
# `scripts/validation/pre_pr.py` for the canonical full-repo call.
# `ignores:` stays in config so explicit walks still honor exclusions.

ignores:
  # Version control and build artifacts. Without these, a `**/*.md` walk
  # recurses through `.git/objects/`, npm cache trees, mypy/pytest/ruff
  # caches, and worktree mirrors -- counting every directory entry
  # along the way. Pre-fix the full-repo lint enumerated 373,381 paths
  # to find 696 real `.md` files. This list narrows the walk to the
  # source tree.
  - ".git/**"
  - "**/node_modules/**"
  - ".venv/**"
  - ".mypy_cache/**"
  - ".pytest_cache/**"
  - ".pytest_tmp/**"
  - ".ruff_cache/**"
  - "ai_agents.egg-info/**"
  - "dist/**"
  # Worktrees are duplicate working copies, not unique source.
  # All gitignored root shapes must be listed here; see .gitignore and issue #4248.
  - ".claude/worktrees/**"
  - "worktree-*/**"
  - "worktree--/**"
  - "wt_*/**"
  - ".wt/**"
  - ".worktrees/**"
  - ".work-*/**"
  - "worktrees/**"
  # Session scratch trees (issue #4892): agent-local working dirs, not
  # authored .md. Untracked, so a sibling session's scratch tree still
  # crashes markdownlint-cli2 (v0.23.1 exits 249 on a 4000+ file arg list
  # with no findings) unless the pattern names it here.
  - ".agent-scratch/**"
  - ".scratch/**"
  # Internal tooling outputs and provider caches: not authored .md.
  - ".agents/**"
  - ".serena/**"
  - ".claude-mem/**"
  - ".forgetful/**"
  - ".diffray/**"
  - ".flowbaby/**"
  - ".factory/**"
  - ".codeql/**"
  - ".baseline/**"
  - ".gemini/**"
  - ".vscode/**"
  - ".config/**"
  - "tmp/**"
  - "**/*.ps1"
  - "**/*.psm1"

  # User instruction files - these are APPENDED to existing user files during installation
  # and cannot start with H1 (MD041). They are simple, controlled content that we manually
  # verify. No inline suppression is used because it would propagate to user files
  - "src/claude/claude-instructions.template.md"
  - "src/vs-code-agents/copilot-instructions.md"
  - "src/copilot-cli/docs/copilot-instructions.md"

  # Prompt templates with intentional nested code blocks and XML-like tags (scratchpad, etc.)
  # that confuse the parser. These are prompt content, not markdown errors
  - "docs/autonomous-pr-monitor.md"
  - "docs/autonomous-issue-development.md"

  # TEMPORARY: Agent definition files (.github/agents/*.agent.md) have pre-existing linting
  # issues being fixed in a separate PR. This exclusion unblocks PR #847.
  # TODO: Remove this exclusion after the linting fix PR merges
  - ".github/agents/**/*.agent.md"

  # CLAUDE.md files are managed by the claude-mem plugin which prepends <claude-mem-context>
  # HTML tags, violating MD033 and MD041. These are tool-managed metadata, not authored markdown.
  - "**/CLAUDE.md"

  # Lifecycle command files have only YAML frontmatter, no H1 heading. MD041 not applicable.
  # Only the 5 lifecycle commands are excluded; other command files retain full linting.
  # /review is excluded from this list because it was converted from a lifecycle
  # command to a proper skill in #2042; its SKILL.md has an H1 heading and is
  # fully linted.
  - ".claude/commands/spec.md"
  - ".claude/commands/plan.md"
  - ".claude/commands/build.md"
  - ".claude/commands/test.md"
  - ".claude/commands/ship.md"

  # Copilot CLI mirrors of the lifecycle command files. Same structure as
  # `.claude/commands/{spec,plan,build,test,ship}.md`: YAML frontmatter
  # followed by `@CLAUDE.md` and prose, with no H1 heading. The Claude Code
  # paths above were excluded; the Copilot CLI mirrors were missed in that
  # PR and only surface when hooks are configured (see CONTRIBUTING.md).
  - "src/copilot-cli/skills/spec/SKILL.md"
  - "src/copilot-cli/skills/plan/SKILL.md"
  - "src/copilot-cli/skills/build/SKILL.md"
  - "src/copilot-cli/skills/test/SKILL.md"
  - "src/copilot-cli/skills/ship/SKILL.md"

  # Reference files extracted from spec.md (issue #3632). These are embedded-prose reference
  # documents: they use heading levels (####, ###) relative to the original document's nesting,
  # not standalone files, so MD041 and MD001 are not applicable. They carry no H1 and their
  # heading hierarchy is intentionally non-monotonic (sub-steps within numbered list items).
  - ".claude/skills/spec-generator/references/spec-step0-gates.md"
  - ".claude/skills/spec-generator/references/spec-prior-art-schema.md"
  - "src/copilot-cli/skills/spec-generator/references/spec-step0-gates.md"
  - "src/copilot-cli/skills/spec-generator/references/spec-prior-art-schema.md"

  # XML specification document stored with .md extension for GitHub rendering.
  # The file is valid XML (skill specification schema), not authored Markdown.
  # Its 4-space-indented XML structure produces false-positive MD046 violations
  # (the XML tags are parsed as indented code blocks) and MD041 (no H1 since
  # the document opens with an XML declaration). Renaming to .xml would break
  # existing skill-discovery tooling that walks *.md. Excluded narrowly.
  - ".claude/skills/cva-analysis/references/SKILL_SPEC.md"
  - "src/copilot-cli/skills/cva-analysis/references/SKILL_SPEC.md"

# Per-glob rule overrides for skill trees.
#
# MD040 (fenced-code-language): 575 violations across .claude/skills/ and
# src/copilot-cli/skills/ (86% of all skill-tree violations). Skill documents
# carry many illustrative bare fences used for generic pseudo-code, output
# templates, and multi-language examples where no single language tag applies.
# Requiring a language tag on these fences would force authors to annotate
# illustrative content that is intentionally language-agnostic or to use
# "text" uniformly, which adds no value for these files.
#
# MD033 (no-inline-html): 132 violations. Skill documents use structured
# XML-like tags (<example>, <phase>, <output>) as semantic markers recognized
# by the AI harness. These tags are not decorative HTML; they are part of the
# skill protocol. A blanket no-inline-html rule would force removal of the
# harness protocol markup.
#
# Measured 2026-07-31: disabling both rules eliminates 707 of 823 violations
# (86%) while retaining full coverage on docs/, README, and all non-skill
# files. Only MD040 and MD033 are scoped off; all other rules (MD041, MD046,
# MD024, etc.) continue to apply to the skill trees.
#
# v0.23.1 supports this via the overrides[].filter + config mechanism
# (documented in README "overrides" section). combine: merge ensures the
# parent config is inherited and only the named rules are overridden.
overrides:
  - filter:
      - ".claude/skills/**"
      - "src/copilot-cli/skills/**"
    config:
      MD040: false
      MD033: false
    combine: merge
