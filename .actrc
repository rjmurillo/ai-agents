# act Configuration
# Local GitHub Actions testing with nektos/act
# Documentation: https://nektosact.com

# Use GitHub-hosted runner images for better compatibility
# Use Medium images for balance between size and tool availability
-P ubuntu-latest=catthehacker/ubuntu:act-latest
-P ubuntu-24.04=catthehacker/ubuntu:act-24.04
-P ubuntu-24.04-arm=catthehacker/ubuntu:act-latest

# Windows runners not available in act - use self-hosted workaround
# This runs the workflow on the host machine instead of in a container
-P windows-latest=-self-hosted

# Enable artifact storage (for upload/download-artifact actions)
--artifact-server-path=.artifacts

# Enable caching support (for actions/cache)
--cache-server-path=.cache

# Use linux/amd64 architecture for compatibility
--container-architecture=linux/amd64
