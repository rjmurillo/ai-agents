{
  "hooks": {
    "PostToolUse": [
      {
        "bash": "_ptr=\"${COPILOT_PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}\"; _warn=\"project-toolkit@ai-agents WARNING: hooks DISABLED (your session is unaffected).\"; if [ -z \"$_ptr\" ]; then echo \"$_warn Plugin root unresolvable (COPILOT_PLUGIN_ROOT and CLAUDE_PLUGIN_ROOT both empty). Reinstall the plugin: copilot plugin install project-toolkit@ai-agents\" >&2; exit 0; fi; _interp=\"python3\"; if ! command -v \"$_interp\" >/dev/null 2>&1; then _interp=\"python\"; fi; if ! command -v \"$_interp\" >/dev/null 2>&1; then echo \"$_warn No Python interpreter found. Install Python >= 3.10: https://www.python.org/downloads/\" >&2; exit 0; fi; if ! \"$_interp\" -c \"import sys; sys.exit(0 if sys.version_info >= (3,10) else 1)\" 2>/dev/null; then echo \"$_warn Python >= 3.10 required but $(\"$_interp\" --version 2>&1) found. Upgrade: https://www.python.org/downloads/\" >&2; exit 0; fi; \"$_interp\" -u \"$_ptr/hooks/PostToolUse/_dispatch.py\"",
        "cwd": ".",
        "matcher": "Write|Edit",
        "powershell": "$_ptr = if ($env:COPILOT_PLUGIN_ROOT) { $env:COPILOT_PLUGIN_ROOT } elseif ($env:CLAUDE_PLUGIN_ROOT) { $env:CLAUDE_PLUGIN_ROOT } else { $null }; $_warn = \"project-toolkit@ai-agents WARNING: hooks DISABLED (your session is unaffected).\"; if (-not $_ptr) { Write-Host \"$_warn Plugin root unresolvable (COPILOT_PLUGIN_ROOT and CLAUDE_PLUGIN_ROOT both empty). Reinstall the plugin: copilot plugin install project-toolkit@ai-agents\" -ForegroundColor Yellow; exit 0 }; $_interp = $null; foreach ($c in @(\"py\",\"python3\",\"python\")) { if (Get-Command $c -ErrorAction SilentlyContinue) { $_interp = $c; break } }; if (-not $_interp) { Write-Host \"$_warn No Python interpreter found. Install Python >= 3.10: https://www.python.org/downloads/\" -ForegroundColor Yellow; exit 0 }; $_ver = & $_interp -c \"import sys; print(sys.version_info[0],sys.version_info[1])\" 2>$null; if ($_ver) { $parts = $_ver.Split(\" \"); if ([int]$parts[0] -lt 3 -or ([int]$parts[0] -eq 3 -and [int]$parts[1] -lt 10)) { $_pv = & $_interp --version 2>&1; Write-Host \"$_warn Python >= 3.10 required but $_pv found. Upgrade: https://www.python.org/downloads/\" -ForegroundColor Yellow; exit 0 } }; & $_interp -u \"$_ptr/hooks/PostToolUse/_dispatch.py\"",
        "timeoutSec": 35,
        "type": "command"
      }
    ],
    "PreToolUse": [
      {
        "bash": "_ptr=\"${COPILOT_PLUGIN_ROOT:-${CLAUDE_PLUGIN_ROOT}}\"; _warn=\"project-toolkit@ai-agents WARNING: hooks DISABLED (your session is unaffected).\"; if [ -z \"$_ptr\" ]; then echo \"$_warn Plugin root unresolvable (COPILOT_PLUGIN_ROOT and CLAUDE_PLUGIN_ROOT both empty). Reinstall the plugin: copilot plugin install project-toolkit@ai-agents\" >&2; exit 0; fi; _interp=\"python3\"; if ! command -v \"$_interp\" >/dev/null 2>&1; then _interp=\"python\"; fi; if ! command -v \"$_interp\" >/dev/null 2>&1; then echo \"$_warn No Python interpreter found. Install Python >= 3.10: https://www.python.org/downloads/\" >&2; exit 0; fi; if ! \"$_interp\" -c \"import sys; sys.exit(0 if sys.version_info >= (3,10) else 1)\" 2>/dev/null; then echo \"$_warn Python >= 3.10 required but $(\"$_interp\" --version 2>&1) found. Upgrade: https://www.python.org/downloads/\" >&2; exit 0; fi; \"$_interp\" -u \"$_ptr/hooks/PreToolUse/_dispatch.py\"",
        "cwd": ".",
        "matcher": "Bash",
        "powershell": "$_ptr = if ($env:COPILOT_PLUGIN_ROOT) { $env:COPILOT_PLUGIN_ROOT } elseif ($env:CLAUDE_PLUGIN_ROOT) { $env:CLAUDE_PLUGIN_ROOT } else { $null }; $_warn = \"project-toolkit@ai-agents WARNING: hooks DISABLED (your session is unaffected).\"; if (-not $_ptr) { Write-Host \"$_warn Plugin root unresolvable (COPILOT_PLUGIN_ROOT and CLAUDE_PLUGIN_ROOT both empty). Reinstall the plugin: copilot plugin install project-toolkit@ai-agents\" -ForegroundColor Yellow; exit 0 }; $_interp = $null; foreach ($c in @(\"py\",\"python3\",\"python\")) { if (Get-Command $c -ErrorAction SilentlyContinue) { $_interp = $c; break } }; if (-not $_interp) { Write-Host \"$_warn No Python interpreter found. Install Python >= 3.10: https://www.python.org/downloads/\" -ForegroundColor Yellow; exit 0 }; $_ver = & $_interp -c \"import sys; print(sys.version_info[0],sys.version_info[1])\" 2>$null; if ($_ver) { $parts = $_ver.Split(\" \"); if ([int]$parts[0] -lt 3 -or ([int]$parts[0] -eq 3 -and [int]$parts[1] -lt 10)) { $_pv = & $_interp --version 2>&1; Write-Host \"$_warn Python >= 3.10 required but $_pv found. Upgrade: https://www.python.org/downloads/\" -ForegroundColor Yellow; exit 0 } }; & $_interp -u \"$_ptr/hooks/PreToolUse/_dispatch.py\"",
        "timeoutSec": 95,
        "type": "command"
      }
    ]
  },
  "version": 1
}
