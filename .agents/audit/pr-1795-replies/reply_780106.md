Verified count is 23, not 25. The marketplace counter at `build/scripts/validate_marketplace_counts.py` (the authoritative source for these numbers) excludes `AGENTS.md`, `CLAUDE.md`, and `*.template.md`. Same logic locally:

```
$ find .claude/agents -maxdepth 1 -type f -name '*.md' \
    ! -name 'AGENTS.md' ! -name 'CLAUDE.md' ! -name '*.template.md' | wc -l
23
```

The `Validate Marketplace Counts` CI gate is green, confirming `marketplace.json` and `plugin.json` descriptions match the counter's output. No change needed.
