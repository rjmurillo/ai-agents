Addressed in 58a03bcd. `validate_manifest()` now wraps `path.read_text()` in its own `try`/`except OSError` block before parsing JSON. Missing/inaccessible files return a clean `Manifest read error for '...': ...` message and surface as exit code 1 rather than crashing CI. Test coverage added: `test_manifest_read_error_returns_clean_message`.
