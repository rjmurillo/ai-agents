Real defect — thanks for catching. The branch was named `fix/plugin-manifest-schema-1793` but #1793 is an unrelated PR (`chore(memory): update pr-review skill observations`), not the issue this PR fixes. There is no issue to "Closes" — the regression source is PR #1773.

Will edit the PR body to remove the spurious `Closes #1793` and clarify "Regresses: PR #1773; Fixed by: this PR". The PIR header already correctly references PR #1795.
