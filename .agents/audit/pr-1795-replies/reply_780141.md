Addressed in db0971b3. Module docstring exit codes updated to match actual behavior:

```
Exit codes:
    0 - All manifests valid
    1 - One or more manifests invalid (includes JSON parse and read errors)
    2 - No plugin.json files found (discovery returned empty)
```

Parse and read errors now correctly contribute to `failures` and exit 1 (which they already did in code; only the doc was wrong). Behavior unchanged.
