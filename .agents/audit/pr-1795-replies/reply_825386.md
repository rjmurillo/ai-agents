Addressed in fc4acb0e. `validate_manifest()` now catches `UnicodeDecodeError` separately and returns a clean `Manifest decode error for '...': ...` message (exit 1). Test coverage: `test_manifest_decode_error_returns_clean_message` (writes UTF-16 BOM bytes and asserts the deterministic error path).
