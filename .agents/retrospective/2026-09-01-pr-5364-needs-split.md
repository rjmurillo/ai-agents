# Retrospective: PR #5364 needs-split

## Failure mode classification

- Primary: `False completion markers` in `.agents/governance/FAILURE-MODES.md` item 4
- Why this fits: several review rounds corrected claims that looked complete at the time, but the
  underlying artifacts still missed API-cost coverage, stale-helper fallback coverage, or accurate
  PR-body evidence counts

## Evidence

| Artifact | Link | Why it matters |
|---|---|---|
| Issue | `#5208` | Original defect scope |
| PR | `#5364` | The branch that crossed the split threshold |
| Review thread | `https://github.com/rjmurillo/ai-agents/pull/5364#pullrequestreview-5074953510` | Active review that required the focused helper mode and docstring fix |
| Review thread | `https://github.com/rjmurillo/ai-agents/pull/5364#pullrequestreview-5075285826` | Follow-up review that corrected the PR-body blast-radius claim |
| Commit | `d7f073cc7` | Focused `--field` helper-mode command change |
| Commit | `091c304a4` | Runtime and helper coverage expansion |
| Commit | `7691cf83d` | Plugin-path bootstrap repair for the extracted helper |
| CI run | `https://github.com/rjmurillo/ai-agents/actions/runs/33482735047/job/99775812353` | Caught the missing plugin-path bootstrap on the extracted helper |

## What happened

PR #5364 started as one behavior fix: forward `--is-bot` so bot-authored PRs can reach tier T5.
The branch then absorbed three follow-on rounds:

1. command-contract repairs around stale and malformed producer output
2. documentation and evidence-count corrections in the PR body and command prose
3. a focused helper-mode follow-up to cut duplicate `get_pr_context.py` cost

That turned one bug fix into a mixed change set across command prose, helper scripts, generated mirrors, and tests.

## Why commit count grew

- Review feedback landed in several waves on different commits.
- The first implementation fixed correctness before API-cost concerns were raised.
- Generated mirrors and runtime coverage changed in later rounds, so each review fix touched multiple files.

## Better split

Two PRs would have been cheaper to review:

1. behavior fix: bot classification, fail-closed handling, and direct tests
2. follow-up cleanup: focused context helper mode, mirror regeneration, and evidence/body corrections

## Commit inventory and split points

### Group A: core behavior fix and correctness follow-ups

`9c1747227`, `9edac1b09`, `3ae5451fd`, `ac08b5fc7`, `d238ca33f`,
`b9c45bf8c`, `5ca88244c`, `1892d1598`, `5ee085f34`, `e08336b5b`,
`1f51b21c9`, `9b60e0ede`, `6f9bbc2a5`, `9a5da2dbd`, `a84649777`,
`83f4ee5dc`, `a6c042ac2`, `5848566b4`, `66b7a3678`, `6559be302`,
`c0ae32ad4`, `c3e76688c`, `96683e9e5`, `616bf72d3`, `744fea552`

These commits all stay on the original issue path: make T5 reachable, fail
closed on malformed author evidence, and align the command, tests, and docs.

### Group B: base refresh merges

`3dac907b8`, `bdfd1c438`, `084e113ca`

These are maintenance merges from `origin/main`, not logical split points.

### Group C: API-budget and closeout follow-up

`d7f073cc7`, `091c304a4`, `678058145`, `426ff6cb7`, `7691cf83d`,
`648f260ca`

This is the clean follow-up seam. These commits add focused helper-mode reads,
mirror sync, needs-split documentation, plugin-path bootstrap repair, and the
stale-helper fallback regression case. This group matches the second PR the
split recommendation proposes.

## Impact

| Area | Severity | Effect |
|---|---|---|
| Review time | Medium | Same bug fix had to be re-read across multiple commits |
| CI cost | Medium | Missing focused coverage and plugin bootstrap were caught after push |
| Artifact trust | Medium | PR body and generated mirrors drifted from the shipped tree between rounds |

## Root cause

The branch accepted follow-up fixes one review wave at a time instead of splitting after the first
behavior change landed. That made the PR absorb API-budget refinement, generated parity repair,
runtime coverage growth, and evidence clean-up in one place.

## Next time

- Land the minimal behavior fix first when the defect is already isolated.
- Treat generated-mirror updates as part of the first patch so later reviews do not reopen parity drift.
- Re-run body-count evidence after each test-shape change before requesting review.

## Remediation

| Action | Owner | Tracking |
|---|---|---|
| Keep bot-author and auto-merge reads on focused helper paths with runtime coverage for stale-helper fallback | PR #5364 | Done in `d7f073cc7` and `091c304a4` |
| Keep extracted helper modules on the canonical plugin-root bootstrap path | PR #5364 | Done in `7691cf83d` |
| Split the next follow-up once the first review wave changes API surface, generated mirrors, and tests together | Next author touching `/pr-autofix` | Use `needs-split` as the trigger to branch a follow-up PR |
