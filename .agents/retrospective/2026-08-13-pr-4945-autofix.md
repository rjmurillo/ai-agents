# Retrospective: PR 4945 Autofix

## Session Info

| Field | Value |
|---|---|
| Date | 2026-08-13 |
| Scope | PR #4945 |
| Task type | CI repair and review response |
| Outcome | Fixed locally, pending final guarded push and completion gate |

## Impact

| Area | Severity | Evidence |
|---|---|---|
| Merge readiness | High | Required `Validate PR` failed |
| Review completion | High | Three unresolved review threads |
| Local push | Medium | Pre-push caught stale QA and branch evidence |

## Timeline

1. Acquired lease `d33a241f-fleet-pr-4945`.
2. Bound live head `45df5f72f2670cfd4d776787afdd7ac922fba631`.
3. Classified PR as T4 with one failed required check and three threads.
4. Read the failed check log. It reported no PR-numbered QA report.
5. Added two regression tests. Both failed against the old batching code.
6. Fixed rendered Windows command measurement and oversized targets.
7. Ran 61 targeted tests, Ruff, and a real markdownlint smoke test.
8. Independent review returned `CLEAN`.
9. Pre-push caught stale QA bindings and a newer `main`.
10. Resolved all three review threads after remote checks passed.
11. Completion gate found an active suppressed UTF-16 measurement finding.
12. Counted rendered command length in UTF-16 code units and added a non-BMP
    regression test.
13. The next completion gate found that the error still labeled those units
    as characters.
14. Renamed the reported unit to UTF-16 code units and reran 25 focused tests.

## Failure Mode

Classification: FM-4, False Completion Markers, from
`.agents/governance/FAILURE-MODES.md`.

QA evidence existed, but its filename did not match the gate's
`*pr-4945*.md` discovery contract. The artifact could not prove the claim it
was meant to support.

## Five Whys

1. Why did `Validate PR` fail? It found no QA report for code changes.
2. Why did it find none? Both reports used issue-number names.
3. Why did naming matter? The gate searches only `*pr-<number>*.md`.
4. Why was this missed? Local verification validated report content, not live
   PR discovery.
5. Why did the push still stop? Pre-push reran session and QA gates against the
   merged branch state.

Root cause: QA evidence was valid in content but invalid at its discovery
boundary.

## Decisions

| Action | Decision | Evidence |
|---|---|---|
| Keep | Lease and live-state wrappers around every mutation | No stale PR mutation occurred |
| Add | PR-numbered QA report before push | Required check discovery contract |
| Modify | Measure the full rendered Windows argv in UTF-16 code units | Review findings on `checks_tooling.py` |
| Modify | Report the measured unit by its exact name | Final suppressed review finding |
| Drop | Version banner in parser fixture | Parser only needs the `Linting:` line |

## Remediation

| Action | Owner | Status |
|---|---|---|
| Add `pr-4945` QA report | This session | Complete |
| Refresh stale QA bindings after code changes | This session | Complete |
| Resolve all three review threads with commit evidence | This session | Complete |
| Fix the active suppressed UTF-16 finding | This session | Complete |
| Name the measured unit in the overflow error | This session | Complete |
| Run completion gate before merge | This session | Pending guarded push |

## Extracted Learning

Statement: Name QA reports with the PR number because
`scripts/ci/check_pr_qa_report.py` discovers `*pr-<number>*.md`.

Atomicity score: 90 percent.

Evidence: [PR #4945](https://github.com/rjmurillo/ai-agents/pull/4945),
commit `ac2599c71d56bd9a0d3b2ed5b00608e24a6716b5`, and the failed
`Validate PR` log.

Persistence: not added to Serena memory. The rule is directly retrievable from
`scripts/ci/check_pr_qa_report.py`, and a duplicate memory would drift.

## Closing

Plus: regression tests separated both review defects from the old behavior.

Delta: QA discovery should have been checked before the first push.

ROTI: 3 of 5. The fix was direct. Two full pre-push runs cost about 18 minutes.

Helped: live binding, targeted tests, and an independent review.

Hindered: stale base state and QA evidence tied to earlier code.

Hypothesis: the UTF-16 regression test and refreshed QA binding will clear the
suppressed-finding and merge-readiness gates on the next head.
