Fixed in 6ca4441.

Added `(?m)` multiline flag to the regex pattern in `Test-ScopeAlignment` at line 226:

```powershell
# Before (bug - undercounts numbered/plain lists)
$reqCount = ([regex]::Matches($requirements, '- \[[ x]\]|^\d+\.|^-\s')).Count

# After (fixed)
$reqCount = ([regex]::Matches($requirements, '(?m)- \[[ x]\]|^\d+\.|^-\s')).Count
```

**Comprehensive test coverage added** (29 new tests, 62 total passing):

**Positive Tests** (15):
- Checkbox items (unchecked, checked, mixed)
- Numbered lists (single/double/triple-digit, sequential, non-sequential)
- Plain list items (single, multiple, varying content)
- Mixed formats (all combinations)

**Negative Tests** (6):
- Empty requirements section
- Malformed patterns (missing space, no period, no space after dash)
- Inline patterns (not at line start)
- Non-list content

**Edge Cases** (6):
- Single item / large scale (1000+ items)
- CRLF vs LF line endings
- Empty lines between requirements
- Leading whitespace handling

**Regression Tests** (2):
- Consistency with `Test-RequirementCoverage` (line 272)
- Before/after fix verification

All tests verify the fix eliminates false positive warnings about requirement count mismatches.
