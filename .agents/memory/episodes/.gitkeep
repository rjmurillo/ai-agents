# Episodes Directory

This directory contains structured episode extracts from session logs.

## Format

Each file is named: `episode-{session-id}.json`

## Schema

See: `.agents/schemas/episode.schema.json`

## Generation

Episodes are extracted by: `scripts/Extract-SessionEpisode.ps1`
