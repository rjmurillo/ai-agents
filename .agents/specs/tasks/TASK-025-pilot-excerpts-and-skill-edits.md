---
type: task
id: TASK-025
title: Skill template compile pipeline, guard allowlist, drift gate, tests (A1)
status: todo
priority: P1
complexity: L
source: GH-5706
related:
  - REQ-021
  - DESIGN-020
  - TASK-024
created: 2026-09-11
updated: 2026-09-11
author: spec
---

# TASK-025: Skill template compile pipeline, guard allowlist, drift gate, tests (A1)

## Done definition

- `build/scripts/skill_templates.py` implements `discover`, `owned_targets`, `check_grammar`, `render`, `compile_all` per DESIGN-020, standard library plus `chevron`. A NO-REGEN skip (in-file token or `.noregen` sidecar) leaves the file unchanged, is reported at WARN in both write and validate mode, and exits 1 in every mode (ADR-108 section 4).
- `build/scripts/generate_skills.py` runs the compile before the copy loop, honors `--what-if`, and exposes `--validate`; module docstring cites ADR-108.
- `build/scripts/build_all.py`: the skills step runs the compile in validate mode under `--check`; `assert_no_claude_writes` takes `allowed_paths` and `run()` passes `skill_templates.owned_targets(repo_root)`.
- `pyproject.toml` lists `chevron==0.14.0` in both dev tables; `uv lock` updated; `tests/test_pyproject_dev_deps_parity.py` passes.
- `scripts/validation/checks_portability.py` gains `validate_skill_template_drift`; `pre_pr_sequence.py` gains `_Gate("Skill Template Drift", ...)` after `Generated Artifact Staleness`; `pre_pr.py` re-exports it; `tests/validation/test_pre_pr_sequence_registry.py` `EXPECTED_ORDER` carries the new name at the same position (that test hardcodes the order and goes red otherwise).
- `tests/build_scripts/test_skill_templates_pilot_scope.py` pins `discover()` on the real tree to a `PILOT` constant (empty set in A1) with a docstring stating that widening it is an owner decision.
- `tests/build_scripts/test_generate_skills_template_compile.py` covers the nine cases in DESIGN-020; `tests/build_scripts/test_build_all.py` gains the allowlist positive and the still-flagged negative; all pass.
- `.agents/governance/GENERATOR-FILES.md`, `build/AGENTS.md:8`, `.claude/rules/generated-artifacts.md:196`, `.claude/rules/templates.md` MUST 1, and `templates/AGENTS.md` carry the one-line updates in DESIGN-020; rule mirrors regenerated by `build_all.py`.
- With no template on disk yet, `uv run python build/scripts/build_all.py --check` exits 0 and `uv run python scripts/validation/pre_pr.py` reports no BLOCKING finding.
- `uv run ruff check build/scripts scripts/validation tests/build_scripts` clean; mypy on changed files clean (`chevron` has no stubs: add a `[[tool.mypy.overrides]]` entry with `ignore_missing_imports` if the global setting does not already cover it).

## Implementation notes

- Every partial must end with exactly one trailing newline (exit 2 otherwise); the substring pin in A2 strips that newline before comparing.
- Grammar check runs before `chevron.render`; chevron renders a missing partial and an unknown variable as empty text silently (probed 2026-09-11).
- Under `--check`, the compile never writes; drift is reported as staleness exit 2.
- Atomic commits, five or fewer authored files each: compile module + generator; build_all + its tests; dependency tables + lock; pre_pr wiring + registry test; governance and rule lines; `templates/AGENTS.md`.
- PR body records the engine decision and reasoning (issue #5706 step 3) and states that issue #5657 is untouched.

## Dependencies

TASK-024 merged, so ADR-108 is on the trunk before the guard exception lands.
