# Retired Phase 1 inventory from issue #1726.
#
# No generator or runtime reads this file. Runtime registrations live in
# .claude/settings.json and .claude/hooks/dispatch_groups.json. Copilot output
# is generated through templates/platforms/copilot-cli.yaml and
# build/scripts/generate_hooks.py.
#
# Keep this empty. Duplicating registrations here creates stale paths and
# conflicting exit semantics.

version: 1
manifest_id: ai-agents-hooks-v1
status: retired
hooks: []
