# Session Handoff - 2026-08-03T14:30:26Z

## Status

- **Issue**: #4123, malformed `system_fingerprint` recorded as absent
- **Branch**: `fix/eval-harness-p2`
- **Task**: Finish the eval harness P2 fixes, including all five review cycles.
- **Phase**: complete
- **Progress**: Fifth-cycle metadata timing, ACP lifecycle, model-id redaction, exact child-event parsing, and typing defects are fixed. Fresh-main pre-PR passes.

## Files Modified

- `scripts/eval/_eval_common.py`: Add a typed malformed-provider-metadata error and quote the persistence contract.
- `scripts/eval/_eval_api_adapter.py`: Propagate malformed metadata without retry and validate the injected transport seam.
- `tests/evals/test_eval_agent_vs_baseline.py`: Add end-to-end malformed, absent, and valid fingerprint cases.
- `tests/eval/test_eval_rule_activation.py`: Exercise both rule-activation fingerprint consumers.
- `.claude/skills/context-optimizer/references/rule-audit-instrument.md`: Clarify the fixed run and sign-count rules.
- `.claude/skills/context-optimizer/references/rule-audit-procedure.md`: Remove the contradictory magnitude threshold.
- `src/copilot-cli/skills/context-optimizer/references/rule-audit-instrument.md`: Regenerated mirror.
- `src/copilot-cli/skills/context-optimizer/references/rule-audit-procedure.md`: Regenerated mirror.
- `.serena/memories/eval-harness-observations.md`: Record the retry-boundary failure mode.
- `.agents/sessions/2026-08-03-session-9011-reconstruct-complete-eval-harness-issue.json`: Record session evidence.
- `.claude/lib/ai_review_common/retry.py`: Sync the canonical non-positive retry guard.
- `src/copilot-cli/lib/ai_review_common/retry.py`: Regenerated plugin copy of the retry guard.
- `scripts/eval/eval-rule-activation.py`: Stop mechanism and judge recovery after malformed provider metadata.
- `tests/eval/test_eval_rule_activation.py`: Assert one-call propagation for both recovery scopes.
- `.agents/sessions/2026-08-03-session-9013-fix-independent-review-defects-eval.json`: Record independent review evidence.
- `scripts/eval/_copilot_cli.py`: Serialize only fixed process error hints and delegate transcript parsing.
- `scripts/eval/_copilot_cli_transcript.py`: Validate transcript content and model metadata in a bounded parser module.
- `tests/eval/test_providers.py`: Add fixed-error and normal/opt-in malformed-model call-count probes.
- `tests/eval/test_eval_rule_activation.py`: Persist both generic error paths and prove credential absence.
- `tests/eval/test_fingerprint_three_state.py`: Flip merged sentinel assertions to the typed refusal contract.
- `.agents/sessions/2026-08-03-session-9014-fix-third-cycle-eval-harness-review.json`: Record third-cycle evidence.
- `scripts/eval/_copilot_cli_acp.py`: Send prompts through ACP stdin and reject disabled-capability requests.
- `scripts/eval/_copilot_cli.py`: Remove all tool access, allowlist child environment, and preserve role boundaries in an untrusted-text envelope.
- `scripts/eval/_anthropic_api.py` and `scripts/eval/_providers.py`: Emit fixed provider error categories without response bodies or causes.
- `scripts/eval/_eval_errors.py`: Keep the metadata exception identity stable across script-style module reloads.
- Eval entrypoints under `scripts/eval/` and `evals/spec-generator-spike/`: Re-raise malformed provider metadata before ordinary recovery.
- `tests/eval/test_copilot_cli_acp.py`: Execute safe and unsafe ACP runtime controls.
- `tests/eval/test_provider_error_redaction.py`: Scan traceback and persisted JSON for credential and prompt fragments.
- `tests/eval/test_malformed_metadata_entrypoints.py`: Assert one-call stops across every recovering harness boundary.
- `.agents/sessions/2026-08-04-session-9015-fix-fourth-cycle-eval-harness-correctness.json`: Record fourth-cycle evidence.
- `scripts/eval/eval-rule-activation.py`: Validate response provenance before any judge request.
- `scripts/eval/_copilot_cli.py`, `_copilot_cli_acp.py`, and `_copilot_cli_transcript.py`: Share one session root, classify ACP errors, and join output readers before classification.
- `scripts/eval/_anthropic_api.py`: Remove raw requested and reachable model ids from errors.
- `scripts/eval/eval-model-panel.py`: Parse the exact child JSON metadata event.
- `tests/eval/test_anthropic_model_default.py` and `test_provider_error_redaction.py`: Cover model-id secrecy and typed HTTP fixtures.
- `.agents/sessions/2026-08-04-session-9016-fix-fifth-cycle-eval-harness-correctness.json`: Record fifth-cycle evidence.

## Decisions Made

- **Use a typed `RuntimeError` subclass**: `call_model` must distinguish malformed provenance from ordinary provider failures before its broad retry catch.
  - Alternatives considered: Error-message categorization, a string sentinel, or generic `RuntimeError`.
  - Reference: Issue #4123 and PR #4402 review thread `PRRT_kwDOQoWRls6V3q2v`.
- **Validate the transport seam twice**: Built-in wrappers validate provider objects, while `call_model` validates injected transports before recording success.
  - Alternatives considered: Trust the `Transport` annotation.
  - Reference: `tests/evals/test_eval_agent_vs_baseline.py::TestTransportsRefuseAMalformedFingerprint`.
- **Keep #4123 open**: PR #4451 now says `Refs #4123` because its string sentinel cannot trigger the read-time type guard.
  - Alternatives considered: Close #4123 or retain the unsupported closing keyword.
  - Reference: PR #4451 body, verified 2026-08-03.
- **Sync in two stages**: `scripts/sync_plugin_lib.py` owns `scripts/` to `.claude/lib/`; `build_all.py` then emits `src/copilot-cli/lib/`.
  - Alternatives considered: Hand-edit either generated copy.
  - Reference: `scripts/sync_plugin_lib.py` and `.agents/governance/GENERATOR-FILES.md`.
- **Extract judge-sample recovery**: Keep both typed rethrows while moving optional metadata and judge recovery branches into focused helpers.
  - Alternatives considered: Accept a C901 increase from 12 to 14 or refactor the full scenario runner.
  - Reference: Commit `808952947`.
- **Never serialize Copilot process output**: Inspect at most 4,096 characters for classification, then emit one fixed safe hint and the integer exit code.
  - Alternatives considered: Printable-ASCII filtering and a 200-character excerpt, which preserved short credentials.
  - Reference: Commit `b83f2be5c`.
- **Treat malformed model metadata as corruption**: Validate before the unverified-model opt-in, which accepts absence but not an unreadable present value.
  - Alternatives considered: Preserve the merged policy that mapped non-strings to missing attribution.
  - Reference: `test_malformed_model_metadata_stops_after_one_*`.
- **Reopen #4123**: PR #4451 merged with `Refs #4123` and a `Partially addressed` claim, so its merge did not prove the required fail-fast behavior.
  - Alternatives considered: Leave the issue closed despite its own linked evidence.
  - Reference: Reopen action verified 2026-08-03T18:40:49Z.
- **Correct #4451 closure evidence**: The PR body uses `Refs #4123`, but squash commit `fa57eb5` contains `Fixes #4123` and closed the issue.
  - Alternatives considered: Treat the PR body as the only closure source.
  - Reference: `git show fa57eb5` and issue state were verified before reopening #4123 again.
- **Use ACP stdin with no tools**: The installed Copilot CLI 1.0.78 exposes `--available-tools`; an empty value removes all tools from model context.
  - Alternatives considered: Permission denial with visible tools, the experimental sandbox, and prompt-bearing argv.
  - Reference: Real `copilot --no-auto-update help permissions`, ACP session probe, and the one-call XPIA probe.
- **Allowlist the child environment**: Keep only runtime, host, certificate, and Copilot authentication variables.
  - Alternatives considered: Inherit the parent and rely on tool denial.
  - Reference: The unsafe control reads `XPIA_INHERITED_SECRET`; the production path cannot.
- **Persist fixed provider categories only**: HTTP status remains for retry classification, while response bodies and causes are discarded.
  - Alternatives considered: Bounded ASCII excerpts, which preserve short tokens and prompt fragments.
  - Reference: Credential, prompt-fragment, traceback, and JSON artifact probes.
- **Validate response metadata before judging**: A mechanism response is not eligible for judge spend until its provenance is recordable.
  - Alternatives considered: Preserve validation during result assembly after judging.
  - Reference: The malformed-response probe observes zero judge calls.
- **Share one session root**: Resolve the explicit session override first, then `COPILOT_HOME/session-state`, then the default home.
  - Alternatives considered: Let the child and reader independently interpret ambient variables.
  - Reference: Explicit-root, home-root, and relative-root probes.
- **Classify ACP errors before retry policy**: JSON-RPC details select one fixed category; authentication is non-transient.
  - Alternatives considered: Treat every ACP error as generic server failure.
  - Reference: Fixed auth-message and one-attempt adapter probes.
- **Require exact child JSON**: Model panel recognizes only the child harness's two-field JSON event.
  - Alternatives considered: Search stderr for the exception class substring.
  - Reference: A prose lookalike remains an ordinary RuntimeError.

## Blocked Items

- **None**: The #4538 repair landed in `origin/main` at `49338f940`; merge `e9259f8cc` restored the count ratchets and final `pre_pr.py` exits 0.

## Next Steps

1. Route the provider, process, environment, and untrusted-input changes to security PIV.
2. Push or open a pull request only after user approval.
3. Keep #4123 open until this typed refusal branch lands.

## Context for Next Session

- The branch merged `origin/main` seven times. Do not rebase without updating the session log's reachable `endingCommit`.
- Mutation runs set `PYTHONDONTWRITEBYTECODE=1` after deleting relevant `__pycache__` directories.
- PR #4402 fixes the archive corruption but retries malformed metadata. PR #4451 uses a string sentinel. This branch is the only reviewed variant that raises through `call_model` and validates injected transports.
- Removing either rule-activation typed rethrow fails its dedicated one-call test. Before the fix, both tests failed because generic recovery consumed the exception.
- Restoring raw stderr serialization fails both the direct credential probe and the persisted-artifact probe. Mapping malformed model metadata to absence fails both normal and opted-in call-count probes.
- Fresh `origin/main` was merged in commits `ccf576d63`, `27a142f86`, `4ad00c94f`, `6ce10f87b`, `cbda17c20`, and `8b5f82e7c`; final divergence was 0 behind before validation.
- Full eval suites pass 2,280 tests with 4 skips. Exact fifth-cycle leak, auth, thread, state, and call-budget probes pass 18 of 18.
- The C901 finding identities match fresh main. This branch lowers `eval-agents.py::main` from 24 to 23 and `eval-knowledge-integration.py::main` from 19 to 18; all new or extracted functions are at or below 10.
- Green-main repair `49338f940` was merged as `e9259f8cc`; final pre-PR reports 46 passed, 0 failed, and 4 skipped.

## Verification on Resume

- [ ] `git status` is clean.
- [ ] Branch is `fix/eval-harness-p2`.
- [ ] `git diff --name-only origin/main...HEAD -- .claude/rules .github/instructions src/copilot-cli/instructions` remains empty.
- [ ] `uv run python scripts/validation/pre_pr.py` still exits 0.
- [ ] `python3 scripts/sync_plugin_lib.py --check` exits 0.
- [ ] `uv run python build/scripts/build_all.py --check` exits 0.
- [ ] `uv run pytest tests/evals/test_eval_agent_vs_baseline.py tests/eval/test_providers.py tests/eval/test_eval_rule_activation.py tests/test_ai_review.py -q` still exits 0.

## Related

- Session log: `.agents/sessions/2026-08-03-session-9011-reconstruct-complete-eval-harness-issue.json`
- Previous handoff: none
- PR: none yet
