# Handoff: epic #5456, v0.7.0 control-plane subtraction, cohort 1

Session date: 2026-09-11. Branches: `refactor/5241-adr-100-advisory-ceilings` (merged), `feat/5456-control-plane-baseline`, `docs/5456-disposition-ledger`.

## State

| Deliverable | PR | State at handoff |
|---|---|---|
| ADR-100 items 2 to 4: `check_atomic_commit` and `detect_scope_explosion` advisory, `SKIP_SCOPE_CHECK` removed | #5723 | Merged as efdfe5b31 |
| Baseline script, tests, committed baseline at main `53ffe92c2` | #5725 | Open; all CodeRabbit threads resolved; CI green on the prior head, re-running on afdbe126b |
| Disposition ledger, memory correction, REQ-022 spec | #5727 | Open; depends on #5725 for baseline paths; merge after it |

Baseline numbers at `53ffe92c2`: canonical owner total 412; always-loaded estimated tokens Claude Code 16,879, Copilot 17,281, Codex 965; declared pre-push budget 3,450 s; capability matrix 0 verified of 22.

Ledger tally: KEEP 4, DELETE 5, EXPERIMENT 6, MERGE 0. Release gates: gate 9 checked; gate 4 unchecked with a named exception for the baseline script; gates 5, 6, 7 blocked on the #5422 to #5426 eval chain.

## Decisions made this session

- The baseline is pinned at `53ffe92c2`, the parent of the #5723 merge, so every subtraction is measured against a pre-cohort tree. The epic's ordering (baseline before first deletion) was violated in commit time and repaired in measurement time; the baseline doc says so.
- The "duplicate pre-push ratchet run" candidate from Serena memory was already fixed on main by PR #5418; recorded KEEP, memory corrected in #5727.
- `scripts/metrics/control_plane_baseline.py` is classified EXPERIMENT, not KEEP: one consumer, wired into no gate, delete at release close if no second consumer appears.
- #5420/#5421 is EXPERIMENT (relocation is not deletion); #5436 is DELETE with zero files removed here because the fix lives in the harness (#5651).
- Spec and plan artifacts (REQ-021 to REQ-023, DESIGN-020 to 022, TASK-024 to 026, ontology, execution plan) were produced through the repo `spec` and `plan` skills at the operator's direction; they add roughly 120 KB under `.agents/` and are counted in the baseline's generated-historical bucket, not as canonical owners.

## Next

1. Merge #5725 once CI is green on afdbe126b (`python3 .claude/skills/github/scripts/pr/merge_pr.py --owner rjmurillo --repo ai-agents --pull-request 5725 --delete-branch`).
2. Merge #5727 after #5725.
3. Post the cohort summary on #5456 and tick gate 9 in the epic body.
4. Next cohort candidates with an owner and no blocker: ADR-100 item 5 (DELETE, construct already gone; close it on #5241), #5420 (EXPERIMENT, needs the deletion-versus-movement measurement before any relocation lands).
5. Gates 5 to 7 need #5423 live probes, which need a machine with both CLIs and paid auth; not startable from this environment.

## Machine note

The main checkout had no lefthook hooks installed at session start (only `*.sample` under the shared `.git/hooks`); `uv run --frozen lefthook install` fixed it. Any commit made before that ran no pre-commit gate.
