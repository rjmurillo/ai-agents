# Session Handoff: 2026-09-16T15:10:00Z

## Status

- **Issue**: #5391, Define authoritative placement contract for rules, skills, agents, and Serena memories
- **Branch**: `feat/5391-placement-contract` (docs, PR #5825) and `feat/5391-placement-validator` (validator, PR #5824, worktree `~/worktrees/ai-agents2-5391-validator`)
- **Task**: Drive both PRs through review to merge. #5824 merges first.
- **Phase**: reviewing
- **Progress**: spec, docs, validator, tests, hook wiring, pre_pr, and push all done. Both PRs open. CI and review not yet green.

## Files Modified

- `.agents/specs/requirements/REQ-028-knowledge-placement-contract.md`, `.agents/specs/design/DESIGN-026-memory-placement-validator.md`, `.agents/specs/tasks/TASK-037-placement-contract-and-validator.md`: spec set (PR #5825).
- `templates/rules/knowledge-persistence.md` and its four mirrors: `## Placement contract` section, MUST item 7, checklist step 4 (PR #5825).
- `.serena/memories/README.md`: `## What belongs here` (PR #5825).
- `tests/build_scripts/fixtures/rule_templates_b2/expected/knowledge-persistence.md`: lossless fixture refresh (PR #5825).
- `scripts/validation/check_memory_placement.py`, `tests/validation/test_check_memory_placement.py`, `lefthook.yml`: validator, 30 tests, `memory-placement` job (PR #5824).

## Decisions Made

- **Extend `knowledge-persistence.md`, not `universal.md`**: epic #5456 decided 2026-09-11 that always-on rules do not grow; any byte change there needs four figure docs refreshed.
  - Alternatives considered: new rule file (rejected, second taxonomy); universal.md section (rejected, always-on growth).
- **Two PRs, validator first**: the docs name the `memory-placement` job and check file. Merging docs first would ship a dangling reference.
- **Two-signal classification**: a governance heading or role contract alone is normative; term density alone is only suspect. Corpus: 993 files, 188 normative warnings, 198 suspect, 0 failing.
- **Upstream paths named by shape in the rule prose**: `scripts/` and `templates/agents/` trip the skill Markdown portability ratchet in the plugin mirror. The rule says "under the validation-scripts tree" instead.
- **`git ls-tree -r -z`**: ci-scripts MUST 9 requires NUL separation.
- **Not in `pre_pr_sequence.py`**: pre-commit is the gate the issue asks for; sibling `memory_index.py` is lefthook-only too.

## Blocked Items

- None. Waiting on CI and reviewer bots.

## Next Steps

1. Watch CI on #5824 and #5825: `gh pr checks 5824 --repo rjmurillo/ai-agents` and `gh pr checks 5825 --repo rjmurillo/ai-agents`.
2. Address review threads with the `pr-review` skill (`pr-review 5824`, then `pr-review 5825`).
3. Merge #5824 first, then #5825. #5825 has no code dependency on #5824, so it needs no rebase.
4. After both merge: delete the worktree `~/worktrees/ai-agents2-5391-validator` with `git worktree remove`, and comment on #5392 and #5393 that the contract is on `main`.

## Context for Next Session

- The validator's docstring cites corpus counts measured 2026-09-16. If #5392 or #5393 lands memory deletions before #5824 merges, the counts drift; they are evidence, not a gate.
- `taste-lints` reports the 479-line validator as a file-size warning (ceiling 500). Do not grow it; split before adding signals.
- The pre-push hook runs the full suite (about 4 minutes). Push with `env -u LSP_DOWN flock "$HOME/src/scratch/locks/push-lock-<slug>.lock" git push`.

## Verification on Resume

- [ ] `git status` clean on both branches
- [ ] Branch matches: `feat/5391-placement-contract` in the main checkout, `feat/5391-placement-validator` in the worktree
- [ ] Both PRs still open: `gh pr view 5824 --json state`, `gh pr view 5825 --json state`
- [ ] Next step #1 is still the correct starting point

## Related

- Session log: discontinued (universal.md MUST 8)
- Previous handoff: none
- PR: https://github.com/rjmurillo/ai-agents/pull/5824 and https://github.com/rjmurillo/ai-agents/pull/5825
