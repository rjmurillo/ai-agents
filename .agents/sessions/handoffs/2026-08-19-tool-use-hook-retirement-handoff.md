# Session Handoff, 2026-08-19

## Status

- **Issue**: none filed, owner directive (retire all tool-call hooks, going to zero)
- **Branch**: `claude/tool-use-hooks-perf-ht7zhx`
- **Task**: Retire all 5 registered PreToolUse/PostToolUse/PostToolUseFailure hooks and amend the governing ADRs
- **Phase**: blocked (structural change verified; awaiting owner decision on a second architecture question)
- **Progress**: registrations and hook sources removed and regenerated clean; 32 tests red pending disposition; ADR not authored

## Files Modified

- `.claude/hooks/dispatch_groups.json`, removed the 3 `plugin-pretooluse-*` groups; only the 2 SessionStart groups remain
- `.claude/hooks/hooks.json`, `PreToolUse` was the **only** event; the plugin manifest is now `"hooks": {}`
- `.claude/settings.json`, removed `PostToolUse` (observation_sync) and `PostToolUseFailure` (memory_capture)
- `.claude/hooks/PreToolUse/invoke_require_subagent_model.py`, deleted
- `.claude/hooks/PreToolUse/invoke_serena_memory_scope_guard.py`, deleted
- `.claude/hooks/PreToolUse/invoke_serena_worktree_scope_guard.py`, deleted
- `.claude/hooks/PostToolUse/invoke_observation_sync.py`, deleted
- `.claude/hooks/PostToolUse/invoke_memory_capture.py`, deleted
- `.github/hooks/require-subagent-model.json`, deleted (standalone Copilot registration; not named in the original brief)
- `src/copilot-cli/hooks/**`, regenerated by `build/scripts/build_all.py`; the whole `PreToolUse/` tree (`_dispatch.py`, `_bootstrap.py`, `_manifest.json`, 3 shims) is gone and `hooks.json` is now `"hooks": {}`
- `tests/hooks/test_{observation_sync,require_subagent_model,serena_memory_scope_guard,serena_worktree_scope_guard}.py`, deleted
- `tests/hooks/test_dispatch_groups_parity.py`, removed 5 `AUTHORIZED_HOOKS` entries
- `tests/test_timeout_hardening_2811c.py`, removed the 3 `observation_sync` cases; `detect_test_coverage_gaps` cases retained
- `tests/test_memory_hook_registration.py`, removed the `PostToolUseFailure` entry and 4 capture tests; re-pointed `test_unanchored_command_fails_from_foreign_cwd` at `invoke_memory_recall.py`

## Decisions Made

- **ADR-084's security carve-out does not block this.** Verified by reading it: the carve-out names `invoke_security_gate.py` and `invoke_security_commit_gate.py` specifically. All three vendored hooks carry `Customer value:` docstrings and are cost/correctness controls. `tool-use-hook-bar.md` states outright that `require_subagent_model` "is not a security boundary; its own docstring says so."
- **Left `scripts/memory_enhancement/hooks/post_tool_call_memory.py` in place.** Deleting `invoke_memory_capture.py` left it with zero production callers, so it is now dead library code with 385 lines of tests. ADR-085 Decision 5 would authorize retiring it ("a component may retire when it has zero active consumers"), but that widens the diff. Flagged, not acted on.
- **Zero vendored hooks is explicitly anticipated by the repo.** `test_the_customer_value_check_examines_a_nonempty_surface` says verbatim: "If the vendored surface was deliberately emptied, delete both tests and say so in the ADR."

## Blocked Items

- **Copilot hook surface is retired entirely, not just 3 hooks.** All 3 vendored hooks were `PreToolUse`, and `PreToolUse` was the only event in the plugin manifest. So the project-toolkit plugin now ships zero hooks to any harness, and the generated Copilot dispatcher infrastructure deletes itself. ADR-085 Decision 5 reserves exactly this: "The dispatcher may be removed or replaced by direct host registrations while hooks remain, but that change requires a new architecture decision." Needs owner ratification before ~20 dispatcher-hardening tests are deleted.
- **`adr-review` six-role debate cannot run.** No `Task`/`Agent` delegation tool is registered in this session (confirmed via `ToolSearch`). AGENTS.md makes the debate mandatory on any ADR edit. Cannot be cleared here.
- **Serena MCP unavailable.** No `mcp__serena__*` tools registered, so the session-end memory write cannot be performed. `.serena/memories/` files are still editable on disk.

## Next Steps

1. Owner ratifies (or rejects) retiring the entire Copilot hook surface and its dispatcher, per ADR-085 D-5.
2. Author ADR-097 covering both decisions: the 5 hook retirements and the dispatcher/Copilot-surface disposition. Record that the mandatory adr-review debate did not run and why.
3. Dispose of the 32 red tests. Categories: 2 vacuity guards the repo authorizes deleting; ~20 Copilot dispatcher tests whose subject is gone (includes the #5013 regression pin `test_pretooluse_bash_payload_never_launches_push_pr_guard`); ~5 count/knowledge tests needing doc updates; 1 prune list; 1 ADR retirement marker.
4. Add retirement markers to ADR-068 / ADR-085 prose naming the deleted hooks (`test_adr_hook_claims.py` enforces the words removed|replaced|retired|superseded).
5. Rewrite `.claude/rules/tool-use-hook-bar.md` "Applying the bar to what ships today" (it documents hooks 1 and 4 as live) and regenerate mirrors with `build/scripts/generate_rules.py`.
6. Update prose in `CONTRIBUTING.md`, `.claude/skills/ai-agents-architecture-contract/` (+ `references/weak-points.md`), `.claude/skills/ai-agents-config-catalog/`, `.claude/skills/agent-harness-reference/`, plus copilot mirrors; and `.serena/memories/hooks/require-subagent-model-gate.md`, `.serena/memories/decision-memory-hooks-registered-directly-not-grouped.md`.
7. Check `scripts/ci/test_installed_plugin_hooks.py` (references the retired hook) and the count ratchets.
8. `uv run python scripts/validation/pre_pr.py` before any push.

## Context for Next Session

- The Claude-side dispatcher `.claude/hooks/invoke_dispatch_claude.py` **survives**, the 2 SessionStart groups in `settings.json` still use it. Only the generated Copilot-side `_dispatch.py` died.
- Runtime cost is identical whether or not the dispatcher machinery is preserved: with zero registered hooks there are no per-tool-call spawns either way. The fork is only about whether the machinery and its hardening tests survive for a future hook.
- Preserving the Copilot dispatcher is probably not available without a new generator feature: the generator drives everything from `.claude/hooks/hooks.json`, and hand-maintaining a generated file is forbidden by `.claude/rules/generated-artifacts.md`.
- `build/scripts/build_all.py` handled the empty case cleanly ("Found 0 Claude event(s)"). It preserved an orphan `markdownlint-safe-config.yaml` under `src/copilot-cli/hooks/PreToolUse/`, which keeps that directory alive. `.claude/hooks/PreToolUse/_bootstrap.py` and `markdownlint-safe-config.yaml` may now be orphans too; not investigated.

## Update: ADR authored, reviewed, accepted (post-handoff)

Since this handoff was written, the parent session (with Agent tool access
this subagent lacked) completed the blocked items:

- **ADR-097 authored and accepted**: `.agents/architecture/ADR-097-zero-tool-use-hooks.md`.
  Covers all three ratified decisions (5-hook retirement, full Copilot
  dispatcher retirement per ADR-085 Decision 5, `post_tool_call_memory.py`
  dead-code retirement). `status: accepted`, `implemented: false` (correct
  per ADR-073, flips true at first merged change).
- **Six-role `adr-review` debate ran**: 2 Accept, 4 Disagree-and-Commit, 0
  Block. Full findings and resolution:
  `.agents/critique/ADR-097-debate-log.md`. Two content defects the debate
  caught were fixed in the ADR itself (an inverted ADR-084 carve-out
  citation; `post_tool_call_memory.py` claimed retired before it actually
  was).
- **`post_tool_call_memory.py` actually retired now**: `git rm` on
  `scripts/memory_enhancement/hooks/post_tool_call_memory.py` and
  `tests/test_memory_hook_capture.py`. This was NOT done when this handoff
  was first written (see "Decisions Made" above, which said it was left in
  place), it is done now.
- **`tests/hooks/test_adr_hook_claims.py` passes**: 360/360, including
  against ADR-097's own prose and a pre-existing gap at
  `ADR-085-cross-harness-permission-surface-asymmetry.md:149` (also fixed).

**ADR-097's own Impact on Dependent Components table is now the
authoritative, prioritized punch list for everything still required** ,
more complete than this handoff's original "Next Steps" list, and it
supersedes that list where they differ. Read the ADR before resuming, not
just this handoff. Priority order for what remains (P0 = currently-red or
gate-blocking, code changes beyond ADR prose):

1. `scripts/validation/validate_hook_anchoring.py`, currently red (exit 2),
   wired into `pre_pr.py`, `lefthook.yml`,
   `.github/workflows/validate-plugin-manifests.yml`. Needs a disposition:
   permit zero hooks as valid, or retire it.
2. `scripts/ci/test_installed_plugin_hooks.py` (+ its action.yml/workflow) ,
   needs redesign, not a rename; `main()` returns 1 by design when zero
   events are registered.
3. Re-accretion ratchet, new ~5-line test asserting zero
   `PreToolUse`/`PostToolUse`/`PermissionRequest`/`PostToolUseFailure`
   registrations with `surface: "plugin"`. Replaces the invariant the
   deleted #5013 pin protected. Does not exist yet.
4. ADR-071 amendment (mark the `require_subagent_model` contract section
   retired) and an ADR-068 retirement note (lines 187, 227, 234, 309, 501
   contain now-false claims about live registrations).
5. Then the rest of the original Next Steps below: rewrite
   `tool-use-hook-bar.md` + regenerate mirrors, fix
   `tests/validation/test_validation_entry_point_imports.py:38` (remove the
   `post_tool_call_memory.py` entry from `MODULE_ONLY_ENTRY_POINTS`),
   update `.agents/specs/hook-protocol.md:55`, prose updates
   (CONTRIBUTING.md, the 4 named skills, `PostToolUse/README.md`), add
   supersession markers to the 2 named `.serena/memories/` files, update
   `tests/evals/rule-scenarios/tool-use-hook-bar.json`, check count
   ratchets, then `uv run python scripts/validation/pre_pr.py`, atomic
   commits, push, confirm the draft PR.

## Verification on Resume

- [ ] `git status` matches "Files Modified" (no unexpected drift)
- [ ] `git rev-parse HEAD` is `e11c7f463e9769ec77f1335c104c3af7d3e82891` or a descendant
- [ ] `uv run --frozen python -m pytest tests/hooks/ tests/build_scripts/ -q` still reports the same 32 failures
- [ ] `uv run --frozen python build/scripts/build_all.py` is idempotent (no further diff)
