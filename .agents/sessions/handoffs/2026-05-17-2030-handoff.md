# Session Handoff — 2026-05-17

## Status

- **Issue**: #2030 — Proposal: Evidence-tiered agent policies (skillbook + eval-grounded confirmation)
- **Branch**: agent/issue-2030-evidence-tiered-policies
- **Task**: Implement the skillbook system (policy registry, CLI, schemas, validator, post-eval hook, seed data, tests, CI workflow, README).
- **Phase**: reviewing
- **Progress**: Implementation complete. PR #2031 open against main. All 10 acceptance criteria met. Awaiting PR review and merge.

## Files Modified

- `.agents/schemas/policy.schema.json`, `tension.schema.json`, `workflow.schema.json`, `evidence-entry.schema.json` — new draft-07 schemas.
- `scripts/skillbook.py` — new CLI (status/confirm/contradict/promote/tension/select).
- `scripts/validation/validate_skillbook.py` — new schema + integrity validator.
- `.agents/skillbook/policies.json`, `tensions.json`, `workflows.json` — new seed data (30 policies / 1 tension / 3 workflows).
- `.agents/skillbook/README.md` — new model documentation.
- `.agents/hooks/post-eval.py` — new eval-to-skillbook evidence bridge.
- `tests/skillbook/` — new pytest suite (100 tests).
- `.github/workflows/skillbook-validation.yml` — new CI schema validation workflow.
- `evals/security-spike/fixtures/F001.json` — tagged with `policy_id`.
- `evals/security-spike/fixtures/README.md` — documents the `policy_id` field.
- `pyproject.toml` — scoped `ANN401` ruff ignore for the validator.

## Decisions Made

- **No `jsonschema` dependency**: the validator carries a small purpose-built draft-07 subset checker.
  - Alternatives considered: adding `jsonschema` to dev deps; PowerShell `Test-Json` in CI.
  - Reference: matches the `scripts/validate_session_json.py` precedent (hand-rolled, no jsonschema).
- **Dropped the issue example's `evidence_tier` field**: it duplicated `tier`; one field avoids a DRY violation.
- **`questioning` status is reachable only at the validated tier**: matches the issue spec text exactly.
- **One policy per all 24 agent personas** (not the 13 of the first pass): the issue says "1 per persona minimum".

## Blocked Items

- None. The work is complete; the only remaining step is external (PR review and merge).

## Next Steps

1. Review and merge PR #2031 (https://github.com/rjmurillo/ai-agents/pull/2031). On merge, issue #2030 closes via `Fixes #2030`.
2. Follow-up issue: wire `select` output into agent prompts or session start so the registry has a consumer.
3. Follow-up issue: wire `validate_skillbook.py` into `scripts/validation/pre_pr.py` for shift-left local validation.

## Context for Next Session

- `scripts/validation/pre_pr.py` reports two pre-existing failures unrelated to this branch: 427 repo-wide markdown lint errors in untouched files, and a `security` agent template drift. Neither is introduced by this PR; do not try to "fix" them here.
- `new_pr.py` has a worktree bug: `get_repo_root()` returns the parent of `--git-common-dir` (the main repo) instead of `--show-toplevel` (the worktree). PR #2031 was created with `--skip-validation` plus an audit reason for that reason; every check was run manually and passed.
- The git worktree needed `git config --worktree core.bare false` at session start (shared config had `core.bare = true` with `extensions.worktreeConfig` on but no per-worktree override).

## Verification on Resume

- [ ] `git status` matches "Files Modified" (no unexpected drift)
- [ ] Branch matches: `agent/issue-2030-evidence-tiered-policies`
- [ ] Blocked items still blocked (none listed; confirm PR not yet merged)
- [ ] Next step #1 is still the correct starting point (PR #2031 still open)
- [ ] `uv run pytest tests/skillbook/` still reports 100 passed

## Related

- Session log: `.agents/sessions/2026-05-17-session-1153-implement-issue-2030-evidence-tiered-agent.json`
- Previous handoff: none (new issue)
- PR: https://github.com/rjmurillo/ai-agents/pull/2031
- QA report: `.agents/qa/2026-05-17-skillbook-issue-2030.md`
- Critique: `.agents/critique/2026-05-17-skillbook-issue-2030.md`
- Retrospective: `.agents/retrospective/2026-05-17-skillbook-issue-2030.md`
