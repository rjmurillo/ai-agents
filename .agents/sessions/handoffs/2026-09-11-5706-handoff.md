# Session Handoff, 2026-09-11

## Status

- **Issue**: #5706, build(skills): compile SKILL.md from mustache templates so guidance is bundled into skills
- **Branch**: none in flight; work landed on `main` through PRs #5724, #5729, #5730, #5726, #5731
- **Task**: spec, plan, ADR amendment, compile pipeline, and the eight-skill pilot for issue #5706
- **Phase**: complete
- **Progress**: A0, A1, A2 merged; issue closed by #5731

## Files Modified

- `.agents/architecture/ADR-108-template-owned-skill-files.md` (new, `implemented: true` since #5726) and `.agents/critique/ADR-108-debate-log.md` (four `adr-review` rounds plus post-consensus edits)
- `.agents/architecture/ADR-107-canonical-skill-contracts-and-harness-projections.md:61-62` (property 1 exception clause, re-anchored guard lines, Related Decisions bullet)
- `.agents/specs/requirements/REQ-003-multi-tool-artifact-build.md` (REQ-003-010, REQ-003-005, REQ-003-008, D4 amended in place; three verification commands corrected)
- `.agents/specs/requirements/REQ-025-skill-guidance-excerpts.md`, `.agents/specs/design/DESIGN-024-skill-guidance-excerpt-sync.md`, `.agents/specs/tasks/TASK-030-skill-excerpt-parity-gate.md`, `TASK-025-pilot-excerpts-and-skill-edits.md`, `TASK-027-pilot-skill-tests-and-governance.md`, `.agents/specs/ontology/skill-guidance-excerpts.md` (renumbered twice after id collisions with #5725 and #5727)
- `build/scripts/skill_templates.py`, `build/scripts/skill_template_grammar.py` (new), `build/scripts/generate_skills.py` (`--validate`), `build/scripts/build_all.py` (`allowed_paths`, check-mode compile), `scripts/validation/checks_portability.py`, `scripts/validation/pre_pr_sequence.py` (`Skill Template Drift` gate), `pyproject.toml` and `uv.lock` (`chevron==0.14.0`)
- `templates/skills/{sync,test,spec,ship,research,plan,checkpoint,build}.SKILL.md.tmpl`, `templates/skills/partials/*.mustache` (six), the eight rendered `.claude/skills/<name>/SKILL.md` and their `src/copilot-cli/skills/<name>/SKILL.md` mirrors
- `tests/build_scripts/test_generate_skills_template_compile.py`, `test_skill_template_grammar.py`, `test_skill_templates_pilot_scope.py`, `test_skill_templates_symlink_security.py`, `test_skill_partials_rule_parity.py`, `tests/skills/<pilot>/test_skill_md_contract.py` (eight), `tests/test_frontgate_crosslink_1927.py`
- `.agents/governance/GENERATOR-FILES.md`, `build/AGENTS.md`, `templates/AGENTS.md`, `.claude/rules/generated-artifacts.md`, `.claude/rules/templates.md`, `.claude/skills/CLAUDE.md`, `.agents/steering/claude-skills.md`, `.agents/governance/SKILL-CREATION-CRITERIA.md`, `.github/CODEOWNERS`
- `.agents/plans/completed/5706-skill-guidance-excerpts.md` (moved from `active/`)

## Decisions Made

- **Mustache compile into `.claude/skills/` with an ADR-107 and REQ-003-010 amendment (D1, owner)**: the owner chose the issue's letter over a no-amendment excerpt design.
  - Alternatives considered: excerpt blocks in the hand-maintained file pinned by a validator (recorded at the end of DESIGN-024); hold.
  - Reference: ADR-108; plan decision log.
- **NO-REGEN under a template-owned file is WARN plus compile exit 1 (`build_all.py` exit 2)**: the sentinel exempts the file from its only drift gate. Diverges from issue #5706 step 10.
  - Reference: ADR-108 section 4, `adr-review` rounds 3 and 4.
- **Widening controls**: `tests/build_scripts/test_skill_templates_pilot_scope.py` pins `discover()` to the eight names; `.github/CODEOWNERS` lists `templates/skills/` and that test. Neither is a hard gate while the ruleset has `require_code_owner_review: false`.
- **Template name and path containment**: slug pattern, existing non-symlink directory, resolved inside the resolved `.claude/skills/` root, root resolved inside the repo root, non-symlink `SKILL.md`.

## Blocked Items

- None.

## Next Steps

1. Owner decision on whether ADR-108 moves from `proposed` to `accepted`; A1 flipped `implemented: true`.
2. Second wave (deferred in REQ-025): migrate more skills by adding a `.tmpl` and widening `PILOT`; consider `references/*.md` templates; consider replacing `chevron` with an in-tree expander.
3. Always-on rule cut (issues #5400, #5492, #4871) now that the pilot proves partials; owner's word first.
4. Spec id allocation raced twice today (#5725 and #5727 took the same next-free ids as this work in parallel). A follow-up on id allocation is worth an issue; not filed here because #5706 forbids filing follow-ups from this work.

## Context for Next Session

- Edit `templates/skills/<name>.SKILL.md.tmpl` or a partial, then `uv run python build/scripts/build_all.py`; never hand-edit a rendered pilot `SKILL.md`. `uv run python build/scripts/generate_skills.py --validate` names drift.
- Partials MUST end with exactly one trailing newline and MUST be verbatim contiguous spans of their `rule-source` rule file (`tests/build_scripts/test_skill_partials_rule_parity.py`).
- The taste count ratchet had zero slack on `main` after #5725; a branch adding any error-severity taste finding fails the merge-tree ratchet even when it passes standalone.
- The pre-push hook refuses non-fast-forward pushes; merge `main` into a PR branch rather than rebasing a published one.
- Pilot byte report (before, on `main` at `cd0f9561d`; after, on #5731): always-on rules unchanged at 56,984 bytes; pilot `SKILL.md` files grew by 380 to 688 bytes each (sync 6581 to 6961, test 11555 to 11965, spec 11027 to 11433, ship 14296 to 14984, research 8526 to 8939, plan 5637 to 6045, checkpoint 4651 to 5136, build 10773 to 11192). No pilot carries a NO-REGEN sentinel.

## Verification on Resume

- [ ] `uv run python build/scripts/generate_skills.py --validate` prints `Templates match their compiled SKILL.md files.`
- [ ] `uv run python build/scripts/build_all.py --check` exits 0
- [ ] `uv run python scripts/validation/check_spec_id_uniqueness.py` passes
- [ ] `gh issue view 5706 --json state` reports CLOSED

## Related

- Session log: discontinued
- Previous handoff: none
- PRs: #5724 (A0), #5729 and #5730 (spec id hotfixes), #5726 (A1), #5731 (A2)
