---
qaVerdict: PASS
qaSessionLog: .agents/sessions/2026-08-10-session-4862-push-pr-guard-postmerge.json
qaCommit: 21dec0a7df263866f0487fb3ea9a4ce87152126b
---

# PR 4862 QA Report

## Verdict

PASS. Every gate ran on `21dec0a7df`, the last code commit on the branch.

## Evidence

Focused suites, both dispatchers exercised for every guard case:

- `uv run --frozen pytest tests/hooks/ tests/build_scripts/ tests/test_new_pr_cli.py
  tests/test_new_pr_validations.py tests/test_new_pr_content_checks.py
  tests/test_new_pr_validation_base.py tests/test_new_pr_warning_reporting.py
  tests/test_push_pr_interpreter_floor.py -q`
  Result: 3,482 passed, 1 skipped.

Full suite:

- `uv run --frozen pytest tests/ -q --timeout=300`
  Result: 26,727 passed, 36 skipped.

Repository gates:

- `uv run --frozen python scripts/validation/pre_pr.py`
  Result: all validations passed.
- `uv run --frozen python build/scripts/build_all.py --check`
  Result: no drift; every Copilot mirror matches its canonical source.
- `uv run --frozen --extra dev ruff check` on every changed Python file: clean.
- `uv run --frozen python .claude/skills/taste-lints/scripts/taste_lints.py` on
  every authored file in this branch: 0 errors. The only error in the touched
  set is `push_guard_base.py`, which this branch does not modify.
- `uv run --frozen --extra dev semgrep --config auto` on the guard test harness:
  0 findings, against 1 blocking before the harness change.

Adversarial probes, each run against both dispatchers in a temporary repository
holding a lookalike `new_pr.py`, and against the merged guard at `5cd72a7dad`
for attribution:

- 15 delegated-execution commands (8 pipelines, 7 Git command runners): all
  exit 2 here, all exit 2 on the merged guard, all exited 0 before the fix.
- 5 unparseable-consumer pipelines (`| (sh)` and variants): all exit 2.
- 3 renamed byte-identical copies handed to a pipeline: all exit 2.
- 8 read-only controls (`git diff -- .../new_pr.py`, `python3 -m pytest
  tests/test_new_pr.py`, `cat X | head`, `(git status)`, `echo hi | (cat)`):
  all exit 0, where the merged guard denied the first two.
- Amplification probe, a 119,677-byte command of 4,600 brace-bomb tokens:
  0.24 seconds and exit 2, against 100.5 seconds before the shared budget.

Python 3.10 floor, the interpreter the hooks must run on:

- `tests/test_push_pr_interpreter_floor.py`: 14 passed against a real CPython
  3.10 interpreter, covering both CLI scripts and the library module.
- `test_guards_run_on_the_minimum_supported_interpreter`: both surfaces run the
  guard on 3.10 and get the expected allow, deny, and out-of-scope verdicts.

## Scope

Covers the push-pr identity guard on both harnesses, its nine new sibling
modules, the `new_pr.py` and `pr_validations.py` split, the hook generator's
companion publication and metadata handling, and the guard test harness.

Not covered: `tests/e2e/test_plugin_load_smoke.py::test_claude_analyst_runtime_uses_exact_allowlist_with_executor_control`,
which fails on an expired local `claude` OAuth session. Reproduced on a pristine
`origin/main` worktree at `6cc4ec4703` with no local changes, so it is
independent of this branch. Filed as #4861.
