---
qaVerdict: PASS
qaSessionLog: .agents/sessions/2026-08-15-session-4917-serena-worktree-scope.json
qaCommit: 173a13381bb640d1be2c1cea33abdae378007b6c
---

# PR 5036 QA Report

## Verdict

PASS. Root cause fix on commit `d795175b6`: both matchers (`^serena-`) were
unanchored on both harnesses, so the guard never fired since it was added,
reported 4 separate times across this PR's review threads. Anchored to
`^mcp__serena__.*$` (Claude) and `^serena-.*$` (Copilot), added harness
tool-name normalization, and expanded the write-tool inventory from 7 to 16
bare names sourced from oraios/serena's editing-tool classes. Merged
`origin/main` (commit `1a010803`) to pick up interleaving upstream changes
(issue #5013) and regenerate the Copilot artifacts against the merged
source. Reverted the unrelated CI-recovery content (issue #4359) flagged as
scope creep in review (commit `173a1338`).

## Scope

- `.claude/hooks/PreToolUse/invoke_serena_worktree_scope_guard.py`
- `.claude/hooks/dispatch_groups.json`
- `.claude/hooks/hooks.json`
- `tests/hooks/test_serena_worktree_scope_guard.py`
- `tests/build_scripts/test_dispatcher_matcher_union.py`
- `src/copilot-cli/hooks/PreToolUse/_manifest.json` and generated shim (regenerated)
- `.agents/architecture/ADR-068-consolidated-hook-dispatcher.md`,
  `ADR-071-plugin-hook-runtime-contract-verification.md`,
  `ADR-085-cross-harness-permission-surface-asymmetry.md` (dated amendments
  for issue #4917's derived metrics change)
- Reverted (out of scope for #4917): `.claude/skills/github/fix-ci.md`,
  `get_pr_checks.py`, `why_pr_blocked.py`, and their Copilot mirrors and tests

## Evidence

- `uv run python -m pytest tests/hooks/test_serena_worktree_scope_guard.py -v`
  - Result: 64 passed
- `uv run python -m pytest tests/build_scripts/test_hook_contract_knowledge.py tests/hooks/test_dispatch_groups_parity.py tests/build_scripts/test_dispatcher_matcher_union.py -q`
  - Result: all passed
- `uv run python -m pytest tests/hooks/ tests/build_scripts/test_hook_contract_knowledge.py tests/build_scripts/test_dispatcher_matcher_union.py tests/build_scripts/test_generate_hooks.py -q`
  - Result: 1780 passed, 2 skipped
- `ruff check .claude/hooks/PreToolUse/invoke_serena_worktree_scope_guard.py tests/hooks/test_serena_worktree_scope_guard.py tests/build_scripts/test_dispatcher_matcher_union.py`
  - Result: All checks passed
- Negative control: reverted the matcher anchoring to the original `^serena-`
  and confirmed `TestMatcherRegressionGuard`'s two tests fail against it
  (proving the new tests would have caught the original bug), then restored
  the fix and confirmed the full suite passes again.
- Guard now blocks 16 write tools on mismatch (was 7), allows reads
  unconditionally, and normalizes both harnesses' tool-name prefixes.
  Fails open on infrastructure errors (no git, no .serena/); fails closed
  when the Serena project root cannot be determined.
