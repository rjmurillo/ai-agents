---
qaVerdict: PASS
qaSessionLog: .agents/sessions/2026-08-20-session-99922-push-gate-value-evaluation.json
qaCommit: f79e70c017e0a253f92d07ff4ba84f84a9f8b449
---

# QA Report: ADR-098 and ADR-099

**SHA**: f79e70c017e0a253f92d07ff4ba84f84a9f8b449
**Session**: 99922, 2026-08-20
**Branch**: `claude/push-gate-value-eval-tu1lju`
**Scope**: two proposed ADRs and one debate log. Documentation only; no executable surface changed.

## What was validated

This change ships no code, so the QA question is not "does it run" but "is every load-bearing claim true." Both ADRs argue from measurements of this repository, and a false measurement would propagate into an implementation that removes real gates. Every factual claim was therefore checked against a primary source before it was written, and again after review challenged it.

## Verification performed

| Claim | Method | Result |
|---|---|---|
| No CI enforcement of the debate mandate | `grep -rq` for `adr-review`, `adr_review`, `debate-log`, `debate_log` across `.github/workflows/` | Confirmed absent. Corpus size 60 files, checked so the empty result is not a miss |
| `audit-hook-bypass.yml` non-blocking | Read line 67 | Confirmed: "Indicators are non-blocking (exit 1 by contract); do not fail the audit" |
| Scope check blocks today | Read `scripts/detect_scope_explosion.py:50` and the return path | Confirmed `BLOCK_THRESHOLD = 50`, code comment reads "50 is allowed; 51+ blocks" |
| `SKIP_SCOPE_CHECK` unverified | Read `scripts/detect_scope_explosion.py:492` | Confirmed: returns 0 on the env var with no verification |
| Ruleset baseline is in-tree | Read `scripts/validation/ruleset_params_baseline.json` and `scripts/ci/ruleset_required_contexts.py` | Confirmed: ruleset 11104075, nine pinned contexts, `required_approving_review_count: 0`, `strict_required_status_checks_policy: false`, measured 2026-08-14 |
| CODEOWNERS coverage | Read `.github/CODEOWNERS`, counted non-comment entries | Confirmed five globs, all review-prompt or review-library paths, none covering workflows, `scripts/validation/`, `lefthook.yml`, or the file itself |
| Drift check one-directional | Read `check_ruleset_params_drift.py` comparison loop | Confirmed: iterates baseline keys only, so additions are invisible |
| `bypass_actors` never fetched | `grep -rn "bypass_actors" scripts/` | Confirmed: zero matches |
| `BOT_PAT` reachable from the tree | Read `.github/workflows/dependabot-approve-and-auto-merge.yml`; counted referencing workflows; searched for `environment:` gates | Confirmed: error text requires classic `repo` scope for a real user identity, token spent on `gh pr review --approve`, 7 workflows reference it, exactly one `environment:` gate exists (`publish.yml:81`, npm, unrelated) |
| Skip-job shape ships here | Read `.github/workflows/passive-context-budget.yml` | Confirmed: `validate-budget` gated on a path-filter output, paired with `skip-budget` on the negation |
| `skip: merge` location | Read `lefthook.yml` | Confirmed at lines 199-200; line 197 is a glob entry. An earlier draft cited 197 and was corrected |
| governance MUST-1 and MUST NOT 1 | Read `.claude/rules/governance.md` lines 12 and 27 | Confirmed both: human approval plus auto-merge prohibition, and no reducing review requirements without a unanimous-consensus ADR |
| PR #908 rationale | Read `.agents/governance/PROJECT-CONSTRAINTS.md:125` | Confirmed: single incident, 59 commits, review slow and merge risky |
| #5099, #5090, #4846, #5103 | Read `.agents/retrospective/2026-08-17-governance-bureaucracy-critical-review.md` | Confirmed all four as characterized |

## Claims withdrawn after checking

Recorded because a review process that only adds findings is not verifying anything.

- **ADR-021 does not mandate cross-vendor review panels.** A reviewer asserted it did and that the six-role panel therefore violated an accepted ADR. `AI-REVIEW-MODEL-POLICY.md` mandates explicit per-job model routing and forbids PASS under insufficient evidence; it does not constrain subagent panel composition. The reviewer withdrew the claim on re-check. Nothing was built on it.
- **Phase 0 is not blocked by an API 403.** An early draft claimed the ruleset state was unknowable from this session. The baseline is committed in-tree. The draft was wrong and was corrected; reading the baseline produced a worse finding than not knowing.

## Document validation

| Check | Command | Result |
|---|---|---|
| ADR numbering | `check_adr_uniqueness.py` | PASS, next free 100 |
| Dash prohibition | Count of U+2014 and U+2013 across all authored files | 0, re-run after every edit |
| Banned vocabulary | Word-boundary count over the 19 terms in `.claude/rules/voice.md` | 0 |
| Session log schema | `validate_session_json.py` | PASS after this report was created and HANDOFF.md was read; two genuine failures were fixed by performing the missing steps, not by editing the record |
| markdownlint | `npx markdownlint-cli2` on ADR-098 | 0 issues, but `.agents/**` is excluded by the project glob so 0 files were linted. Recorded as not-evidence; the dash and vocabulary checks above are what actually covered these files |

## Adversarial review

Four rounds, six roles, verdicts recorded in `.agents/critique/ADR-098-099-enforcement-planes-debate-log.md`. Rounds 1 through 3 returned blocking findings that changed the design twice and caught two defects in the draft that would have shipped harm:

1. The deadlock mechanism the plane model rests on was factually wrong: a job-level `if:` skip satisfies a required check rather than deadlocking it.
2. ADR-098 would have shipped a tightening inside a subtraction, removing the only relief from a gate that still blocks at 51 files.

Round 4 returned 6 accept, 0 block.

## Known limitations

- **ADR-098's blocking-value evidence is an n of 1.** Four of five sampled pull requests are allow-cases. No wider counterexample search was run: the tooling available could not enumerate pull requests by label. Mitigated by a 90 day re-measure trigger written into the ADR, not resolved.
- **The review panel is single-vendor.** All six roles pin Anthropic models, so correlated blind spots are not addressed. Recorded in ADR-099 as accepted residual risk.
- **No implementation was validated**, because none was written. Both ADRs are proposals; their implementation pull requests carry their own QA obligations.
