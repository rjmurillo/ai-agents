---
qaVerdict: PASS
qaSessionLog: .agents/sessions/2026-08-15-session-5058-fix-4847-midnight-milestones.json
qaCommit: 9b156bd6cf7b032618bc01b769456fe2d202e094
---

# PR 5058 QA Report

## Verdict

PASS. All memory tests pass on commit `13d891b1ed36bb8676e23209893c602ba0f6bea4`.

## Scope

- `.claude/skills/memory/scripts/extract_session_episode.py`
- `src/copilot-cli/skills/memory/scripts/extract_session_episode.py`
- `tests/skills/memory/test_extract_session_episode.py`
- `tests/skills/memory/test_episode_causal_preservation.py`
- `tests/skills/memory/test_repair_episode_causal_links.py`
- `scripts/quality_gate/consume_pytest_signal.py`

## Evidence

- `uv run pytest tests/skills/memory/ -x -q`: 486 passed
- `uv run ruff check` on all changed files: all checks passed
- Pre-push hooks: all passed (python-tests 28818 passed, 38 skipped)
- Issue #4847 root cause: `_event_order_relation` treated midnight < commit_time as causal ordering. Fix returns None (incomparable) for synthetic midnight events.
- Backward compatibility: real-timestamped milestones still produce edges; only synthetic midnight milestones are affected.

## Refresh note (2026-08-15, landing coordinator)

Merged `origin/main` (one conflict in a quality-gate script, resolved to main's encoding-only subprocess arguments) and fixed the open review finding in `9b156bd6c`: the repair tool's raw edge-count guard refused any rewrite that lowered the count, which preserved the exact false milestone-to-commit edge the tool exists to remove. The guard now compares edge sets and permits a rewrite whose removed edges all join events `_event_order_relation` classifies as incomparable under post-restamp timestamps; removed edges between still-comparable events remain refused as evidence. Discrimination proven by a mutation control (refuse-all behavior back in place fails `test_false_milestone_to_commit_edge_is_removed`; its still-comparable control passes either way). The Copilot mirror was regenerated via `build_all`, not hand-edited. Re-verified on the merged tree: 488 memory-suite tests pass (12 in the repair file, 2 new), `check_generated_staleness.py` rc=0, taste (583), ruff (27), and type-ignore (44) ratchets unchanged.
