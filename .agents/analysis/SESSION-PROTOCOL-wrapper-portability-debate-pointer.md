# SESSION-PROTOCOL Wrapper Portability Debate

Canonical debate log:
`.agents/critique/SESSION-PROTOCOL-wrapper-portability-debate-log.md`.

This pointer exists because the commit hook still searches `.agents/analysis/`
while the ADR review skill requires `.agents/critique/`. Issue #4250 tracks the
contract mismatch.
