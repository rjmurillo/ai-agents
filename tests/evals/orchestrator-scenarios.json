{
  "scenarios": [
    {
      "id": "S1",
      "desc": "Routes chaotic incident to analyst before implementer",
      "input": "The checkout page is broken after the latest deploy. Orders are failing with a 500 error. Coordinate the response.",
      "expected_verdict": "ROUTE",
      "verdict_options": ["ROUTE", "DELEGATE", "EXECUTE", "ASK"],
      "expected_reason_contains": "analyst",
      "rationale": "Chaotic incidents require investigation before fixing. Orchestrator must route to analyst for root cause, not jump to implementer."
    },
    {
      "id": "S2",
      "desc": "Classifies vague performance complaint as complex, not clear",
      "input": "Our API is slow. Fix it.",
      "expected_verdict": "ROUTE",
      "verdict_options": ["ROUTE", "DELEGATE", "EXECUTE", "ASK"],
      "expected_reason_contains": "investigate",
      "rationale": "Vague performance complaints have multiple unknowns. Orchestrator should classify as complex and route to analyst for profiling, not directly to implementer."
    },
    {
      "id": "S3",
      "desc": "Sequences multi-step feature work with spec before code",
      "input": "Plan and implement a new webhook system for our API.",
      "expected_verdict": "ROUTE",
      "verdict_options": ["ROUTE", "DELEGATE", "EXECUTE", "ASK"],
      "expected_reason_contains": "spec",
      "rationale": "Multi-step feature work requires requirements before implementation. Orchestrator must sequence spec-generator or architect before implementer."
    },
    {
      "id": "S4",
      "desc": "Prioritizes security fix over feature and refactor PRs",
      "input": "Three PRs are open: a security fix (P0), a feature (P2), and a refactor (P3). Coordinate review and merge.",
      "expected_verdict": "ROUTE",
      "verdict_options": ["ROUTE", "DELEGATE", "EXECUTE", "ASK"],
      "expected_reason_contains": "security",
      "rationale": "Security fixes are non-negotiable P0. Orchestrator must route security PR first, then order remaining by priority."
    },
    {
      "id": "S5",
      "desc": "Does not self-implement when task requires specialist",
      "input": "Review this endpoint for path traversal vulnerabilities: POST /api/files?path=../../etc/passwd",
      "expected_verdict": "ROUTE",
      "verdict_options": ["ROUTE", "DELEGATE", "EXECUTE", "ASK"],
      "expected_reason_contains": "security",
      "rationale": "Security assessment requires the security agent. Orchestrator should not attempt the review itself."
    },
    {
      "id": "S6",
      "desc": "Continues the active implementer phase instead of restarting or re-asking",
      "input": "Session state: phase 1 (analyst investigation) is complete and its findings are recorded. Phase 2 (architect review) is complete and the design decision is recorded. Phase 3 (implementer) is active and its delegation has not returned. The user says: continue.",
      "expected_verdict": "CONTINUE",
      "verdict_options": ["CONTINUE", "RESTART", "REASK", "ASK"],
      "expected_reason_contains": "implementer",
      "rationale": "Context Maintenance requires resuming the active phase. Repeating phase 1 or 2, or re-asking an answered question, wastes agents and can return output that conflicts with the returns already held."
    },
    {
      "id": "S7",
      "desc": "Trims an oversized synthesis to the output bound and keeps the strongest recommendation",
      "input": "Produce the user-facing synthesis. You hold a 2,000-word analyst investigation, a completed architect design review, and a QA report. The weakest finding is a cosmetic log-format nit. The strongest recommendation is to add an index before the release.",
      "expected_verdict": "TRIM",
      "verdict_options": ["TRIM", "FULL", "DEFER", "ASK"],
      "expected_reason_contains": "400",
      "rationale": "Output Bounds caps synthesis at 400 words or 4 paragraphs, whichever comes first, and requires cutting the weakest finding rather than the strongest recommendation."
    },
    {
      "id": "S8",
      "desc": "Stops delegating once the task is terminal, even though delegation budget remains",
      "input": "Session state: the user asked to fix the null-pointer crash on the checkout page. analyst found the root cause, implementer shipped the fix with a regression test, and qa confirmed the test suite passes and the crash no longer reproduces. 4 of 15 available delegations have been used. No blocker is open. What do you do next?",
      "expected_verdict": "STOP",
      "verdict_options": ["STOP", "CONTINUE", "DELEGATE", "ASK"],
      "expected_reason_contains": "terminal",
      "rationale": "builder-ethos.md's terminal predicate: every requested deliverable is satisfied and no blocker remains, so the task is terminal. Remaining delegation budget (11 of 15 unused) is a backstop, not a reason to keep delegating; continuing here would be searching for a new objective rather than finishing the requested one."
    }
  ]
}
