{
  "scenarios": [
    {
      "id": "S1",
      "desc": "Produces working code with tests for a clear requirement",
      "input": "Implement a rate limiter middleware for an ASP.NET Core API. Requirements: 100 requests per minute per IP, sliding window, return 429 with Retry-After header.",
      "expected_verdict": "PRODUCE",
      "expected_reason_contains": "test",
      "rationale": "Clear requirements must produce code and tests. Missing tests would indicate TDD discipline failure."
    },
    {
      "id": "S2",
      "desc": "Fixes security vulnerability without scope creep",
      "input": "The current code uses string concatenation for SQL queries in 3 places. Fix the SQL injection vulnerability.",
      "expected_verdict": "PRODUCE",
      "expected_reason_contains": "parameterized",
      "rationale": "Implementer must fix the specific vulnerability with parameterized queries, not migrate to an ORM or redesign the data layer."
    },
    {
      "id": "S3",
      "desc": "Includes edge case tests, not just happy path",
      "input": "Add a health check endpoint that verifies database connectivity and returns degraded status if the DB is slow (>500ms).",
      "expected_verdict": "PRODUCE",
      "expected_reason_contains": "timeout",
      "rationale": "Health checks must handle DB-down and slow-response edge cases. Only testing the happy path misses the point."
    },
    {
      "id": "S4",
      "desc": "Does not skip tests even when not explicitly asked",
      "input": "Write a retry policy for HTTP calls to an external API. Requirements: exponential backoff, max 3 retries, jitter, circuit breaker after 5 consecutive failures.",
      "expected_verdict": "PRODUCE",
      "expected_reason_contains": "unit test",
      "rationale": "TDD discipline means tests ship with code. Implementer must produce tests for retry count, backoff timing, and circuit breaker state even without being told."
    }
  ]
}
