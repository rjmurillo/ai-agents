---
status: recorded
process: adr-review multi-agent debate (5 reviewers + prior architect gate)
adr: ADR-082
---

# ADR-082 Debate Log

Multi-agent review of ADR-082 (Claude-side consolidated hook group
dispatch), session 3044, workflow `wf_b7a28376-2dd`. The architect gate ran
separately first (see `2026-07-16-adr-082-architect-review.md`,
APPROVE-WITH-CHANGES, conditions resolved).

## Votes

| Reviewer | Vote |
| --- | --- |
| architect (prior gate) | APPROVE-WITH-CHANGES |
| critic | ACCEPT_WITH_CONDITIONS |
| independent-thinker | ACCEPT_WITH_CONDITIONS |
| security | ACCEPT_WITH_CONDITIONS |
| analyst | ACCEPT_WITH_CONDITIONS |
| high-level-advisor | ACCEPT |

No REJECT votes. Consensus: accept with the conditions below.

## Major findings and resolutions

1. critic: `gate_all` shim that blocks AND emits a decision document had
   the document silently dropped while the ADR claimed stderr logging.
   RESOLVED: `claude_hook_dispatch.py` now logs the blocking shim's
   decision document to stderr;
   `test_gate_all_blocking_shim_decision_document_logged_to_stderr` pins it.
2. critic + independent-thinker: unrecognized JSON (and, worse, the LSP
   guards' standalone `{"systemMessage": ...}` warn documents) classified
   as terminal decision documents in gate mode, silently skipping later
   gates in the group. RESOLVED: advisory-only documents
   (`systemMessage`/`suppressOutput`) now merge as context and never
   terminate the group
   (`test_standalone_system_message_does_not_terminate_gate_group`);
   truly unrecognized JSON still passes through verbatim but with a
   stderr warning (`test_unrecognized_json_is_terminal_but_warned`).

## Minor findings and dispositions

- Self-host bail keys on plugin-name equality; a consumer plugin also
  named `project-toolkit` would bail silently. Documented in ADR
  consequences; path-equality hardening deferred as follow-up.
- Linux-only timing probe; ADR cites ADR-068's 246 ms Windows cold-start
  figure for extrapolation. Windows before/after measurement remains an
  open acceptance criterion of issue #3075.
- cwd fallback in `_project_self_hosts_plugin` documented in code (fails
  safe to running the group).
- ADR status stays Proposed until the PR-side adr-review workflow and
  maintainer acceptance; this debate log is the pre-commit evidence.
