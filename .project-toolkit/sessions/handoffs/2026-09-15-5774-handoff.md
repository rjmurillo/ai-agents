# Session Handoff, 2026-09-15

## Status

- **Issue**: #5774, docs: per-directory guides stale after ADR-109 B1; compress to agent-first density; add a `.claude/` guide
- **Branch**: `claude/agent-first-docs-dt4apj`, draft PR open against `main`
- **Phase**: PR open, awaiting review; CI running on the pushed head
- **Related issues filed this session**: #5788 (stale sibling docs and rule text), #5789 (`detect_agent_drift.py` default `--claude-path` compares 0 of 31)

## State of the guides

Every guide re-verified against `main` as of ADR-109 B5 (`4d83781fe`), rewritten in the PR #5713 template under a byte ceiling, one owning guide per shared fact.

| Guide | Bytes at 9a96a5f | Bytes at head |
|---|---|---|
| `.agents/AGENTS.md` | 6241 | 3354 |
| `.claude/AGENTS.md` | 0 | 3800 |
| `.claude/hooks/AGENTS.md` | 11430 | 5768 |
| `.claude/skills/CLAUDE.md` | 6745 | 3806 |
| `.claude-mem/memories/AGENTS.md` | 1343 | 1889 |
| `.github/AGENTS.md` | 8299 | 4869 |
| `build/AGENTS.md` | 8166 | 5497 |
| `docs/project-structure.md` | 8363 | 4653 |
| `scripts/AGENTS.md` | 7644 | 4068 |
| `src/AGENTS.md` | 4813 | 2800 |
| `src/claude/AGENTS.md` | 6757 | 2971 |
| `templates/AGENTS.md` | 6516 | 5800 |
| `tests/AGENTS.md` | 10463 | 5205 |

Ownership: `templates/AGENTS.md` owns the per-class render map; `build/AGENTS.md` owns generator order, `OWNED_PREFIXES`, binplace, gate semantics, the lib render; `.claude/AGENTS.md` owns the `lib/` split; `docs/project-structure.md` owns navigation only.

## Decisions made this session

- `.claude/AGENTS.md` is reached through the map and one pointer line in `.claude/CLAUDE.md`, never an `@AGENTS.md` import, so the always-on budget stays flat.
- Root `AGENTS.md` keeps `PS 7.5+` (workflows and `git_hook_policy.py` invoke `pwsh`) and drops `Pester 5.7+` (no workflow enables it, zero tracked PowerShell); pytest floor is 9 per `pyproject.toml`.
- `Weekly retro -> reflect` in `CLAUDE.md` was audited as disagreeing with `reflect`'s own SKILL.md; left as the owner's explicit routing choice.
- Rule files, `templates/README.md`, and `GENERATOR-FILES.md` staleness went to #5788 rather than widening the PR; the drift-gate default path went to #5789 as a code defect.

## Next steps

1. Drive the PR to green: CI on the pushed head, review threads, Claude Approvals.
2. Done: main landed #5794 (skill support-file mirror) and #5791 (B6 marketplace switch) after the PR opened; both were merged in and the affected guides realigned. The ADR-109 program is complete, so no further base moves of that kind are expected.
3. After merge, close #5774; #5788 and #5789 stay open.

## Traps recorded this session

- The pre-push `merge-tree-ratchet` fails closed on any merge conflict with `origin/main` and reports it as a ratchet failure; merge main first, then push.
- The pre-push `Citation Freshness (added lines)` gate rejects a `file:NNN` citation whose anchor text moved; guides now cite file names, not line numbers.
- `push-ref-policy` refuses a shallow clone; `git fetch --unshallow origin` is the remedy.
- Any uncommitted file under `OWNED_PREFIXES` (`src/`, `.claude/hooks/`, `.claude/lib/`, and more) reds `build_all.py --check` as regen drift; commit, do not regenerate.
- `tests/test_workspace_limits.py` caps root `CLAUDE.md` at 3000 bytes; a merge from main can push it over.
- The workflow runner in this environment has two concurrent agent slots; a 40-agent workflow takes hours, and main moved three times during one run.

## Related artifacts

- PR #5713 and issue #5712 (the template), ADR-109, `.agents/governance/GENERATOR-FILES.md`, `templates/platforms/binplace.yaml`
- Retrospective: none written this session; `.project-toolkit/retrospective/2026-09-11-agent-first-docs-rewrite.md` covers the prior rewrite
