Addressed in this commit. `validate_manifest()` now rejects when `name` is missing OR not a non-empty string. Catches `int`, `None`, empty string, and whitespace-only.

Test coverage: `test_name_must_be_non_empty_string` parametrizes over `(123, None, "", "   ")` and asserts each surfaces the schema error. 34 tests pass.
