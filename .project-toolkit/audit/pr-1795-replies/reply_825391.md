Addressed in fc4acb0e. `_validate_hooks` referenced-file load now catches `(OSError, UnicodeDecodeError, json.JSONDecodeError)` and returns a single "unreadable" message. Test coverage: `test_referenced_hooks_decode_error_caught`.
