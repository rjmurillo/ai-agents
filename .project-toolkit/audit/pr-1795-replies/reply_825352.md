Addressed in fc4acb0e. `find_manifests` now uses `os.walk` with in-place pruning of `dirnames` so excluded directories (`node_modules`, `.git`, `worktrees`, `cache`) are never descended. Test coverage: `test_find_manifests_prunes_node_modules`. Local runs no longer scan irrelevant deep trees.
