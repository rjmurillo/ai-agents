Addressed in ba7778af. `src/claude/` plugin now declares `"agents": "./"` so auto-discovery scans the plugin root where the agent `.md` files live. Validator's new `_validate_relative_path()` enforces the `./` prefix going forward.
