Addressed in db0971b3. `.github/actions/validate-plugin-manifests/action.yml` now installs `pytest==8.3.3` (pinned). Step renamed to "Install pytest (pinned for deterministic CI)" so the intent is explicit. Future upgrades will be a deliberate version bump rather than passive drift from upstream releases.
