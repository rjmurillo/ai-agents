Addressed in 58a03bcd. Validator now requires `agents`/`skills`/`commands` paths to start with `./` and rejects `..` traversal via the new `_validate_relative_path()` helper. Test coverage added: `test_path_field_without_dot_slash_prefix_rejected`, `test_path_field_with_traversal_rejected`. Repo manifests updated to use `"./"` (was `"."`) in `src/claude/` and `src/copilot-cli/` to satisfy the rule.
