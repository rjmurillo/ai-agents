Addressed in 58a03bcd. `_validate_hooks` now calls `_validate_relative_path("hooks", value)` after the `.json` extension check, applying the same `./` prefix and `..` traversal rules to the string-path form. Test coverage: `test_hooks_string_path_must_have_dot_slash`, `test_hooks_string_path_no_traversal`.
