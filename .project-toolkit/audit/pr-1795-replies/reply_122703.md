Addressed in 698cbb17. Validator now strictly requires the canonical wrapper:

- Without `"hooks"` key at top level → returns clear error explaining Claude Code does not load events without the wrapper, with example.
- Test coverage: `test_referenced_hooks_must_have_top_level_wrapper`.

This now matches the captured schema notes and the production examples (caveman, context-mode, security-guidance). 33 tests pass.
