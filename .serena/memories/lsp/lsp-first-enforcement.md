# LSP-First Navigation Enforcement (ADR-062)

**Status**: Implemented by PR #2168 (merge commit `96aafb4fa`, session 1857, 2026-05-31). Adapted from `nesaminua/claude-code-lsp-enforcement-kit` (MIT, v2.3.2), a JS kit, ported to Python.

## What it is

Conditional, availability-gated LSP-first navigation enforcement. Prefer Serena MCP symbolic tools, then native LSP (Claude `LSP` tool / Copilot auto-LSP), then grep/glob/sed as last resort. Block grep/Read/Glob for code symbols ONLY when an LSP is available; fail open otherwise (never deadlock). See `.agents/architecture/ADR-062-conditional-lsp-first-enforcement.md` and its `.agents/critique/ADR-062-debate-log.md` (verdict ACCEPTED-WITH-DC).

## Components

- Shared lib (`scripts/hook_utilities/`, synced to `.claude/lib/hook_utilities/` via `scripts/sync_plugin_lib.py`):
  - `lsp_provider.py`: `detect_providers(file_path, capability, project_dir)`, pure config check (no probe). Capability split is load-bearing: `SYMBOL_NAVIGATION` -> programming languages only (py/ts/bash/powershell); `SYMBOLS_OVERVIEW` -> all 8 `.serena/project.yml` languages incl md/json/yaml/toml.
  - `lsp_symbols.py`: `is_code_symbol` (verbatim kit port), grep-command parsing, git-grep allow.
  - `lsp_gate_state.py`: SoR for gate-state. State OUTSIDE working tree: `$XDG_STATE_HOME/ai-agents-lsp-gate` or `~/.cache/ai-agents-lsp-gate` (file `lsp-gate-<sha256(cwd)[:16]>.json`). NAV_REQUIRED=2, FREE_READS=2, WARN_AT=3.
- 8 hooks: PreToolUse `invoke_lsp_{grep,bash_grep,glob,read,pre_delegation}_guard.py`; PostToolUse `invoke_lsp_usage_tracker.py` and `invoke_lsp_read_tracker.py`; SessionStart `invoke_lsp_session_reset.py`.

## Key invariants

- Exit codes: 0=allow (incl fail-open + warn), 2=block (Claude hook semantics, ADR-035 exemption).
- Every guard fails open: tty/empty/malformed/missing-field/no-provider/exception -> exit 0.
- `SKIP_LSP_GATE=true` kill switch (mirrors SKIP_QA_GATE). `LSP_GATE_MODE=warn` downgrades block to advisory.
- Symbol-grep guards key on SYMBOL_NAVIGATION (never fire on md/json/yaml/toml). Read gate keys on SYMBOLS_OVERVIEW (gates all 8 langs per user directive). Two distinct capabilities in `lsp_provider`.
- The two PostToolUse trackers are the state writers: the usage tracker records navigation through `record_nav`, and the read tracker records allowed reads through `record_read`. Together they keep one gate-state SoR without inter-component inference.

## Registration + generation

- Registered in BOTH `.claude/settings.json` (relative paths) and `.claude/hooks/hooks.json` (`${CLAUDE_PLUGIN_ROOT}`). New matchers: Grep, Glob, Read, Agent; bash-grep appended after the existing Bash chain; usage-tracker regex matcher (Serena tools + native LSP); read-tracker matcher `Read`; session-reset appended to the SessionStart Python block.
- Copilot mirror generated by `build/scripts/generate_hooks.py` from settings.json -> `src/copilot-cli/hooks/`.
- Steering rule `.claude/rules/lsp-first.md` mirrored by `build/scripts/generate_rules.py` to `.github/instructions/` + `src/copilot-cli/instructions/`.
- Merged plugin manifests (PR #2168 historical snapshot; pre-bump source `710d343e9`, landed source `96aafb4fa`): `.claude/.claude-plugin/plugin.json` 0.4.73 to 0.5.6; `src/copilot-cli/.claude-plugin/plugin.json` 0.4.84 to 0.5.6. The durable invariant is the parity gate: both manifests must always hold the same version; the specific numbers are point-in-time evidence, not a live constraint.

## Validation

PR #2168 historical snapshot (point-in-time counts, not a live invariant): 1314 hooks tests passed; lib 100% coverage; guards 95-100%. pre_pr.py: 12 checks passed (em-dash, install-parity, plugin-bump, canonical-citation all PASS); the 2 failures were preexisting and unrelated to this work (agent drift in security/task-decomposer; markdownlint debt in build/audit/GENERATION-AUDIT.md and powershell-security-checklist.md).

## Dogfooding proof

The bash-grep guard blocked a real `grep get_today_session <file>.py` during this session (Serena available + snake_case symbol + .py target), demonstrating the conditional block fires correctly in production.

## Architect-review dissent (recorded)

Review recommended warn-first/measure-then-flip (the #1993 soft layer is 1 day old; ADR-061 withdrawal precedent). User chose hard-block enabled on merge; mitigated by kill switch + fail-open + LSP_GATE_MODE + audit.log.
