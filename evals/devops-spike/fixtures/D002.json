{
  "schemaVersion": 1,
  "id": "D002",
  "policy_id": "pol-devops-force-alternatives-cache",
  "input": "Workflow review: Proposed cache step keys on '${{ runner.os }}-${{ hashFiles(everything csproj) }}'. Before approving, list 2 alternative cache-key strategies and the trade-off vs each.",
  "provenance": "synthetic",
  "assertions": [
    {
      "kind": "verdict",
      "expected_value": "IDENTIFY"
    },
    {
      "kind": "regex",
      "pattern": "(?i)(alternative|lockfile|package-lock|requirements\\.txt|composite.*key|coarser|finer|2\\.|second.*strat|no.*cache)"
    }
  ],
  "tags": [
    "agent-devops",
    "triad-aware",
    "verdict-identify",
    "agent-discriminating"
  ]
}
