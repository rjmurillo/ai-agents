{
  "schemaVersion": 1,
  "id": "D005",
  "policy_id": "pol-devops-ci-script-shell-injection",
  "input": "Review this composite action step:\n```yaml\nrun: echo \"PR title: $TITLE\"\nenv:\n  TITLE: ${{ github.event.pull_request.title }}\n```\nPR titles are user-controlled. Verdict?",
  "provenance": "synthetic",
  "assertions": [
    {
      "kind": "verdict",
      "expected_value": "IDENTIFY"
    },
    {
      "kind": "regex",
      "pattern": "(?i)(injection|untrusted|user.*controlled|escape|GITHUB_ENV|env.*variable.*safe|safe.*pattern|no.*direct.*expansion|already.*safe|env.*var.*pattern)"
    }
  ],
  "tags": [
    "agent-devops",
    "synthetic-pinned-from-public",
    "verdict-identify"
  ]
}
