{
  "schemaVersion": 1,
  "id": "Q003",
  "policy_id": "pol-qa-missing-negative-tests",
  "input": "PR adds a function `validate_session_id(sid: str) -> bool`. Tests: `validate_session_id('abc123')` returns True (3 assertions). What is missing?",
  "provenance": "synthetic",
  "assertions": [
    {
      "kind": "verdict",
      "expected_value": "IDENTIFY"
    },
    {
      "kind": "regex",
      "pattern": "(?i)(negative.*test|invalid.*input|empty|None|too.*long|special.*char|whitespace|reject|false.*case|both.*branch)"
    }
  ],
  "tags": [
    "agent-qa",
    "synthetic",
    "verdict-identify"
  ]
}
