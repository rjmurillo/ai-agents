{
  "schemaVersion": 1,
  "id": "Q006",
  "policy_id": "pol-qa-ok-thorough-test-plan",
  "input": "PR description has Test plan section with: 5 unit tests (happy path, null input, empty input, max-length input, special-char input), 2 integration tests against real DB, 1 negative-path test that asserts the right exception type. Coverage 100 percent. Verdict?",
  "provenance": "synthetic",
  "assertions": [
    {
      "kind": "verdict",
      "expected_value": "OK"
    },
    {
      "kind": "regex",
      "pattern": "(?i)(thorough|good.*coverage|approve|nothing.*missing|complete|ship|OK)"
    }
  ],
  "tags": [
    "agent-qa",
    "synthetic",
    "verdict-ok"
  ]
}
