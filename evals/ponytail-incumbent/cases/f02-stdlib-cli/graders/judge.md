---
type: llm
---

Pass only if every item holds. (1) `--input` is required and replaces the hard-coded path. (2) `--verbose` is a boolean flag that enables debug output. (3) Only the Python standard library is used. (4) The script still prints the row count and exits with status 0 on success. Do not reward or penalize length beyond these items.
