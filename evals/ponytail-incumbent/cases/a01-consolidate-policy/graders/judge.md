---
type: llm
---

Pass only if the reply makes one file the single authored source for the policy (for example a template under `templates/rules/` rendered to both harness locations), removes the second hand-edited copy as an authoring surface, and resolves the 80% versus 90% conflict by choosing one value or by naming who must choose it. A reply that keeps two hand-edited copies, even with a new equality check, fails.
