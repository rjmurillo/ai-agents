# Passive Context Byte Budget Validation
#
# Validates repository-owned root instruction files against local byte
# ratchets accepted under issue #4880.
#
# Exit codes: 0 = pass, 1 = budget exceeded
#
# IMPORTANT: This workflow runs on ALL PRs to satisfy required status checks,
# but skips actual validation when no relevant files are changed.

name: Passive Context Budget

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]
  push:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  check-paths:
    name: Detect changes
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 5
    outputs:
      should-run-budget: ${{ steps.determine.outputs.should-run-budget }}
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Check path filters
        uses: dorny/paths-filter@7b450fff21473bca461d4b92ce414b9d0420d706 # v4.0.2
        id: filter
        if: github.event_name != 'workflow_dispatch'
        with:
          filters: |
            context:
              - 'AGENTS.md'
              - 'CLAUDE.md'
              - '.claude/CLAUDE.md'
              - '.github/copilot-instructions.md'
            validator:
              - 'scripts/validate_workspace_budget.py'
              - 'tests/test_workspace_limits.py'
              - 'tests/test_validate_workspace_budget.py'
              - 'tests/test_root_instruction_layers.py'

      - name: Determine if budget validation should run
        id: determine
        env:
          GH_EVENT_NAME: ${{ github.event_name }}
          FILTER_OUTPUTS: ${{ toJson(steps.filter.outputs) }}
          FILTER_KEYS: context,validator
          OUTPUT_NAME: should-run-budget
        run: python3 scripts/workflows/determine_should_run_from_filters.py

  validate-budget:
    name: Validate budget
    needs: check-paths
    if: needs.check-paths.outputs.should-run-budget == 'true'
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup Python
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97 # v7.0.0
        with:
          python-version: '3.14'

      - name: Run budget validator
        run: python3 scripts/validate_workspace_budget.py --path .

  skip-budget:
    name: Skip budget (no changes)
    needs: check-paths
    if: needs.check-paths.outputs.should-run-budget != 'true'
    runs-on: ubuntu-24.04-arm
    timeout-minutes: 1
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Skip message
        run: |
          echo "No passive context changes detected - skipping budget validation"
