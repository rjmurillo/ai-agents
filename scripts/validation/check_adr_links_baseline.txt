# Known broken ADR links that this gate reports but does not block on.
#
# Each entry is <kind>:<file>:<target>, keyed without a line number so an
# unrelated edit above the link does not invalidate the entry. kind is
# required: a bare <file>:<target> key conflates every violation class that
# can name the same pair, so an allowance for one kind (say, unresolved)
# would also silently hide a different, newly introduced kind (say,
# number-mismatch) on the identical file and target (PR #5209 review,
# discussion_r3831835196). 2 of the 18 entries below are `absolute`, not
# `unresolved`, which is exactly the mixed baseline that check was written
# against (re-measured after the round-5 fix removed a stale `absolute`
# entry for docs/search-dont-load.md; Copilot, PR #5209 round-6 review
# caught the two counts this comment had not been updated to match).
# `tests/validation/test_check_adr_links.py::test_baseline_header_counts_match_the_live_file`
# re-measures both counts against this file directly, so a future edit here
# without updating this line fails the suite instead of drifting silently
# again. Every entry is a real defect. They are recorded rather
# than fixed because issue #5197 scoped the repair to the ADR corpus and
# these live in files owned elsewhere.
#
# Remove an entry when its link is repaired. MUST NOT add an entry to clear a
# link the current change introduced (.claude/rules/ci-scripts.md MUST NOT 4
# applies to this file for the same reason it applies to a count ratchet).
# Both halves are enforced now, not just written down: check_adr_links.py
# exits 2 on an entry this branch added that the base ref does not carry, and
# exits 2 on a full-corpus scan that leaves an entry unmatched. A repaired
# link's entry is not inert, so deleting it is required rather than tidy: the
# key carries no line number, so a stale entry silently suppresses the next
# finding that shares its kind, file, and target.
#
# The historical roots in scripts/validation/stale_script_refs.py HISTORICAL_ROOTS
# are exempt in code and need no entry here.

# Owner decision. ADR-021 cites ../critique/ADR-019-debate-log.md for its own
# debate history. No such file exists and none ever did: `git log --all` over
# both *ADR-019-debate-log* and *ADR-021-debate-log* returns no commits, and no
# artifact under .agents/critique/ (193 files) or .agents/archive/ matches the
# described debate (sessions 86 to 90, "5 Accept + 1 Disagree-and-Commit").
# Sessions 86 to 90 in .agents/archive/sessions/ are the ADR-017 debate. The
# reference is left in place rather than deleted or invented; the owner decides
# whether to locate the artifact or replace the citation with prose.
unresolved:.agents/architecture/ADR-021-model-routing-strategy.md:../critique/ADR-019-debate-log.md

# .agents/architecture/reviews/ is a review archive, not one of the roots
# stale_script_refs.py exempts. Every target is written repo-root-relative with
# a leading ./, which resolves under .agents/architecture/reviews/ instead.
unresolved:.agents/architecture/reviews/ADR-041-codeql-integration-REVIEW.md:./.agents/architecture/ADR-005-powershell-only-scripting.md
unresolved:.agents/architecture/reviews/ADR-041-codeql-integration-REVIEW.md:./.agents/architecture/ADR-006-thin-workflows-testable-modules.md
unresolved:.agents/architecture/reviews/ADR-041-codeql-integration-REVIEW.md:./.agents/architecture/ADR-035-exit-code-standardization.md
unresolved:.agents/architecture/reviews/ADR-041-codeql-integration-REVIEW.md:./.agents/architecture/ADR-041-codeql-integration.md

# Repo-root-relative targets written without the leading ../ hops needed from
# the containing directory.
unresolved:.agents/audit/pr-req003-body.md:.agents/architecture/ADR-006-thin-workflows-testable-modules.md
unresolved:.agents/audit/pr-req003-body.md:.agents/critique/ADR-006-amendment-2026-04-28-debate-log.md
unresolved:.agents/metrics/workflow-coalescing.md:.agents/architecture/ADR-026-pr-automation-concurrency-and-safety.md
unresolved:scripts/eval/README.md:.agents/architecture/ADR-023-quality-gate-prompt-testing.md
unresolved:scripts/eval/README.md:.agents/architecture/ADR-057-prompt-behavioral-evaluation.md

# Stale slugs. ADR-006 is ADR-006-thin-workflows-testable-modules.md and
# ADR-042 is ADR-042-python-migration-strategy.md.
unresolved:.agents/guides/python-cicd-patterns.md:../architecture/ADR-006-thin-workflows.md
unresolved:.agents/security/benchmarks/README.md:../../architecture/ADR-042-python-first.md

# Leading-slash targets. GitHub resolves a leading slash against the site root,
# not the blob tree, so these render broken. ADR-037 is also a stale slug:
# the file is ADR-037-memory-router-implementation.md.
absolute:.agents/specs/PRD-memory-enhancement-layer-for-serena-forgetful.md:/.agents/architecture/ADR-007-memory-first-architecture.md
absolute:.agents/specs/PRD-memory-enhancement-layer-for-serena-forgetful.md:/.agents/architecture/ADR-037-memory-router-architecture.md

# Issue #2796: the four `unresolved` entries formerly here for
# memory-enhancement/SKILL.md are gone, not just re-pathed. The comment they
# carried ("one ../ hop short... repairing the .claude file and regenerating
# fixes both") was itself wrong: .claude/skills/memory-enhancement/ sits three
# levels below repo root but the generated src/copilot-cli/skills/memory-enhancement/
# mirror sits four, so no single ../ count resolves the same relative link in
# both trees. Converted both ADR citations to the repo's dominant
# backtick-path convention (see agent-harness-reference/SKILL.md:346-347)
# instead, which is depth-independent and already used by every other
# cross-plugin-root citation in this codebase.
